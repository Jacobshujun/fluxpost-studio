# Handoff

Last updated: 2026-06-16

## New Session Reading Order

1. `AGENTS.md`
2. `docs/harness/handoff.md`
3. `docs/harness/progress.md`
4. `docs/harness/feature_list.json`
5. `docs/harness/decisions.md`
6. `docs/harness/project_brief.md` when product scope, stack, data, APIs, or deployment are involved
7. `docs/harness/pitfalls.md` and `docs/harness/architecture_rules.md` before code or deployment changes
8. `docs/harness/verification.md` before completion

## Current Task

Bug fix completed on 2026-06-16 for failed simple link-mode run `simple-1781606587581` using Dongchedi URL `https://www.dongchedi.com/ugc/article/7650456390460555326`. Read-only diagnosis showed the run imported one Dongchedi source but generated 0 posts because simple production skipped it as having no `images`, `downloadedImages`, or `videoFrames`. The persisted raw record was a comment/reply object with empty `image_urls`; a no-write live HTML parse showed the actual article body lives under `articleData.data` and has 6 content images.

Root cause: `src/lib/dongchedi.ts` could rank comment records above the article body because recursive title lookup treated nested `profile_info.name` as a title, while large numeric `group_id` values lost precision before matching the requested article id. The fix excludes Dongchedi comment/reply records from article selection, reads title/body from direct article fields, recognizes string id fields such as `group_id_str`, and gives article-specific fields such as `cover_list` priority. `scripts/harness/dongchedi_import_check.mjs` now includes a mixed article/comment fixture to guard this exact failure.

Verification passed: `node scripts/harness/dongchedi_import_check.mjs`; `node scripts/harness/link_import_check.mjs`; `node scripts/harness/simple_link_run_check.mjs`; `node scripts/harness/source_import_feishu_check.mjs`; `npx --no-install tsc --noEmit`; `npm run lint` with the existing 3 warnings only; full `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`; read-only `npm run db:diagnose -- --limit 5 --logLimit 10`; no-write live Dongchedi HTML parse with media caching mocked confirming the failed URL now normalizes as `mediaType="image"` with 6 images; and `npm run local:restart`, which refreshed `http://127.0.0.1:3001/`. No runtime data mutation, media download, TikHub call, OpenAI-compatible text/image call, image-provider generation, or Feishu write was triggered. The historical failed run remains failed in runtime data; rerunning the task from the UI is the next live validation path if needed.

Bug fix completed on 2026-06-16 for the main production UI image-strategy controls and GPT-Image-2 size dispatch. In `src/app/page.tsx`, the default production strategy, draft-generation, and manual image-regeneration controls now use `production-setting-grid` instead of fixed `sm:grid-cols-[minmax(0,1fr)_...]` layouts that could compress the left description block into single-character vertical wrapping in narrow panes. `src/app/globals.css` adds the adaptive grid and `production-setting-summary` styling so controls keep a practical minimum width and wrap as rows when the pane is narrow.

The same task removed the remaining old route-level `1200x1600` image-size fallback. `src/app/api/generate/route.ts` and `src/app/api/images/route.ts` normalize incoming sizes with `normalizeImageGenerationSize(...)` and pass the normalized value into `generateImagesFromPrompt(...)` and execution-log details. `scripts/harness/image_task_fallback_check.mjs` now checks both routes for normalized dispatch and absence of the old fallback.

Verification passed: `node scripts/harness/image_task_fallback_check.mjs`; `npx --no-install tsc --noEmit`; `npm run lint` with existing 3 warnings only; full `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`; elevated `npm run local:restart` after sandbox `spawn EPERM`; and elevated Playwright CSS/layout smoke on `http://127.0.0.1:3001/` confirming a 330px test grid rendered with `161px` summary/select widths, no horizontal overflow, `/api/config` 200, and no console errors. The local production app is refreshed at `http://127.0.0.1:3001/`. No live TikHub, OpenAI-compatible text/image provider, image-provider generation, Feishu production write, runtime data mutation, or generated media mutation was triggered.

Follow-up bug fix completed on 2026-06-16 for the `/review` left-side `批量审查` panel after the initial scroll fix. `src/app/globals.css` now renders `.review-list` as a vertical flex list with a fixed minimum card height, so generated-post cards no longer compress/stack on top of one another when many rows are present. A mocked Playwright check with 195 generated posts confirmed `.review-list` is `display:flex`, visible cards have `minCardHeight=152`, no overlap, scroll advances to `24505`, and there is no horizontal overflow or console error.

The same follow-up also fixes manual Feishu publish enqueue failures logged as `Generated post not found`. Root cause: Feishu queue enqueue previously rewrote each selected `GeneratedPost.ownerUserId` to the current queue owner before local persistence; when an admin/operator published visible posts whose stored owner differed from the current account, `saveGeneratedPost(...)` rejected the update. `src/lib/feishu-publish-queue.ts` now preserves generated-post ownership during enqueue while still using the signed-in user as the Feishu queue owner. Regression coverage in `scripts/harness/workspace_accounts_check.mjs` and `scripts/harness/feishu_publish_queue_check.mjs` guards this boundary.

Verification passed: `node scripts/harness/workspace_accounts_check.mjs`; `node scripts/harness/feishu_publish_queue_check.mjs`; `npx --no-install tsc --noEmit`; `npm run lint` with the existing 3 warnings only; `npm run local:restart`; the mocked Playwright `/review` layout check described above; and full `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`. The local production app is refreshed at `http://127.0.0.1:3001/`. No live Feishu write, TikHub, OpenAI-compatible text/image provider, image-provider call, or runtime data mutation was triggered.

Implementation completed on 2026-06-16 for global GPT-Image-2 image-size options. The app no longer exposes the old free-form `1200x1600` style image-size input. `src/lib/image-size-options.ts` centralizes the selectable actual request sizes and ratio labels: `1024x1536` (2:3, default), `1536x1024` (3:2), `1024x1024` (1:1), `1536x864` (16:9), and `3840x2160` (16:9 4K). Workspace prompt settings normalize legacy/arbitrary values to the nearest supported request size, and `src/lib/image-generation.ts` dispatches only the shared normalized request size.

UI wiring in `src/app/page.tsx` now renders global/default production settings, single-post generation, regeneration, and manual image generation size controls as selects backed by the same option list. Verification passed: `node scripts/harness/image_task_fallback_check.mjs`; `node scripts/harness/workspace_accounts_check.mjs`; `node scripts/harness/feishu_publish_queue_check.mjs`; `npx --no-install tsc --noEmit`; `npm run lint` with the existing 3 warnings only; `npm run build` with known Turbopack tracing warnings; full `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`; read-only `npm run db:diagnose -- --limit 5`; and `npm run local:restart`. The local production app is refreshed at `http://127.0.0.1:3001/`. No live TikHub/OpenAI-compatible text/image provider, image-provider, Feishu production write, or runtime data mutation was triggered. `scripts/harness/workspace_accounts_check.mjs` was also aligned with the current Feishu publish queue invariant that enqueueing preserves each generated post's original owner rather than rewriting ownership to the queue owner.

Bug fix completed on 2026-06-16 for the `/review` left-side `批量审查` list not scrolling downward. The fix is limited to `src/app/globals.css`: `.review-sidebar` is now a vertical flex container, and `.review-list` has `min-height: 0` plus desktop `flex: 1 1 auto`, so the generated-post list scrolls independently inside the left column while the search/filter/batch controls remain at the top.

Verification passed: `npx --no-install tsc --noEmit`; `npm run lint` with the existing 3 warnings only; full `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`; `npm run local:restart` after sandbox `spawn EPERM` required approved elevated execution; and an elevated Playwright mocked `/review` browser check on `http://127.0.0.1:3001/review` with 42 intercepted generated posts confirmed `.review-list` had `clientHeight=440`, `scrollHeight=982`, `scrollTop` advanced to `542`, `overflow-y=auto`, no horizontal overflow, and no console errors. The local production app is refreshed at `http://127.0.0.1:3001/`. No live TikHub, OpenAI-compatible text/image provider, image-provider, Feishu production write, or runtime data mutation was triggered.

Implementation completed on 2026-06-16 for Dongchedi source-link/ID import, including the user-provided source `https://www.dongchedi.com/ugc/article/7643008384274546713`. Dongchedi is a `SourceLinkPlatform`, not a keyword `CrawlPlatform`: advanced link import and simple link mode accept full `/ugc/article/{id}` URLs, auto-detect Dongchedi URLs, and accept pure numeric article ids when the selected platform is `dongchedi`.

The local importer lives in `src/lib/dongchedi.ts`. It canonicalizes to `https://www.dongchedi.com/ugc/article/{sourceId}`, parses embedded article JSON from HTML, normalizes title/body/author/images/video/metrics/publish time into `NormalizedSourceItem` with `platform="dongchedi"`, caches media through the existing media-cache path, and fails clearly when Dongchedi returns a ByteDance anti-bot challenge page instead of article data. Wiring was added across `src/lib/source-link-import.ts`, `src/lib/types.ts`, `src/app/api/crawl/links/route.ts`, `src/lib/simple-runs.ts`, `src/app/page.tsx`, `src/lib/source-import-feishu.ts`, `src/lib/feishu-cli.ts`, `src/app/review/page.tsx`, `src/lib/mock-data.ts`, and `src/lib/lark-task-launcher.ts`; Harness coverage lives in `scripts/harness/dongchedi_import_check.mjs` and is wired into `scripts/harness/check.ps1`.

Verification passed: `node scripts/harness/dongchedi_import_check.mjs`; `node scripts/harness/xiaopeng_bbs_import_check.mjs`; `node scripts/harness/link_import_check.mjs`; `node scripts/harness/simple_link_run_check.mjs`; `node scripts/harness/source_import_feishu_check.mjs`; `node scripts/harness/lark_task_launcher_check.mjs`; `npx --no-install tsc --noEmit`; `npm run lint` with existing 3 warnings only; `npm run build`; full `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`; read-only `npm run db:diagnose -- --limit 5`; and `npm run local:restart`. The local production app is refreshed at `http://127.0.0.1:3001/`. No live Dongchedi import, TikHub, OpenAI-compatible text/image provider, image-provider, or Feishu production write was triggered.

Important next entry point: if a real Dongchedi URL import fails with the anti-bot challenge message, treat that as current platform access behavior rather than an empty-item import. Do not move Dongchedi into TikHub keyword crawl controls unless a separate verified provider path is added.

Implementation completed on 2026-06-16 for user-customizable audit prompts on `/distribution-check`. The page now loads `distributionCheckPrompt` from workspace settings, lets the signed-in operator edit/save it, and provides a default-prompt restore action. The default prompt lives in `src/lib/distribution-check-prompt.ts`; workspace settings persist and normalize it through `src/lib/workspace-settings.ts` and `src/app/api/workspace/settings/route.ts`.

The distribution-check API reads workspace prompt settings and passes the current request prompt, falling back to the saved prompt, into `runDistributionCheck(...)`. `src/lib/distribution-check.ts` uses the custom prompt only for the model assessment text and keeps backend hard guards in place: JSON-only output, `distribution` limited to `可分发`/`不可分发`, and uncertainty normalized to `不可分发`.

Verification passed: `node scripts/harness/distribution_check.mjs`; `node scripts/harness/json_check.mjs package.json tsconfig.json docs/harness/feature_list.json`; full `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`; read-only `npm run db:diagnose -- --limit 5`; and `npm run local:restart`. The local production app is refreshed at `http://127.0.0.1:3001/`. No live Feishu writes were triggered.

Bug fix completed on 2026-06-16 for Weibo crawl duplicate image variants entering downstream AI processing. `src/lib/media-url-filter.ts` now derives a dedicated Weibo image asset key for `sinaimg.cn` content-image size directories, including `woriginal`, `oslarge`, `large`, `mw*`, `hmw*`, `cmw*`, `chmw*`, `orj*`, `orh*`, `or*`, `hlarge`, `bmiddle`, `middleplus`, `wap*`, `thumbnail`, `thumb*`, `small`, `square`, and `crop.*`; it scores Weibo variants so original/full-size URLs beat mobile/preview variants. `src/lib/tikhub.ts` also recognizes Weibo `woriginal`/`oslarge`/`cmw*`/`wap*` image fields during recursive content-image extraction. Regression coverage was added to `scripts/harness/media_url_filter_check.mjs` and `scripts/harness/weibo_search_mapping_check.mjs` for `large`/`wap360`/`wap180`/`cmw960` collapse and `oslarge`/`woriginal` collapse.

Verification passed: `node scripts/harness/media_url_filter_check.mjs`; `node scripts/harness/weibo_search_mapping_check.mjs`; `npx --no-install tsc --noEmit`; read-only runtime sample for historical `weibo-5310146915074476` showed current normalization reducing `images` from `18` to `5` without mutating runtime data; and full `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1` with existing 3 lint warnings and known Turbopack tracing warnings only. No TikHub, OpenAI-compatible text/image provider, Feishu write, runtime data mutation, generated media mutation, or historical-content rewrite was triggered by verification.

Read-only diagnosis completed on 2026-06-16 for the user-reported Weibo crawl image duplication issue where the same image appears in multiple resolutions and then enters AI processing. Root cause: `src/lib/tikhub.ts` correctly funnels Weibo image extraction through `normalizeContentImageUrls(...)`, but `src/lib/media-url-filter.ts` does not collapse several Weibo CDN size directories such as `wap360`, `wap180`, and `cmw960` to the same asset key as `large`. Current checks cover `large`/`hmw2000`/`orh1080`/`crop`/`thumbnail`, but not those live variants. Read-only runtime sampling through `FLUXPOST_DIAG_DATABASE_URL` checked 52 recent Weibo source items and found `weibo-5310146915074476` with `images=18`, current normalized count still `18`, and 5 basename-duplicate groups; one group contained the same filename under `large`, `wap360`, `wap180`, and `cmw960`. Focused checks run: `node scripts/harness/weibo_search_mapping_check.mjs`, `node scripts/harness/media_url_filter_check.mjs`, read-only `npm run db:diagnose -- --limit 5 --logLimit 20`, and a no-write local normalization repro. No TikHub, OpenAI-compatible image/text provider, Feishu write, runtime data mutation, generated media mutation, or business-code change was triggered.

Recommended next entry point: update `src/lib/media-url-filter.ts` Weibo asset-key normalization to recognize Weibo CDN size directories including at least `wap\d+`, `cmw\d+`, `mw\d+`, `oslarge`, and `woriginal` as variants of the same basename, and prefer the highest-quality URL in `scoreImageUrl(...)` (`large`/`woriginal`/`oslarge`/`mw2000` above preview/mobile sizes). Add regression coverage to `scripts/harness/weibo_search_mapping_check.mjs` and/or `scripts/harness/media_url_filter_check.mjs` for a `large`/`wap360`/`wap180`/`cmw960` fixture and an `oslarge`/`woriginal` fixture. After code changes, run the focused checks plus `npx --no-install tsc --noEmit` and the full Harness baseline; restart `http://127.0.0.1:3001/` only if the running local production server must reflect the fix.

Bug fix completed on 2026-06-16 for the `/distribution-check` field preflight error `unknown flag: --format`. `lark-cli base +field-list` on the local CLI supports `--jq` but not `--format`, so `src/lib/distribution-check.ts` now calls field-list with `--jq "."`. `scripts/harness/distribution_check.mjs` guards this CLI compatibility rule. Verification passed: live read-only field-list with `--jq "."`; `node scripts/harness/distribution_check.mjs`; `npx --no-install tsc --noEmit`; full Harness baseline; read-only `npm run db:diagnose -- --limit 5`; and `npm run local:restart`. The local production server at `http://127.0.0.1:3001/` is refreshed with the fix.

Implementation completed on 2026-06-16 for the Feishu Base `是否分发` distribution audit mini-tool. New page `/distribution-check` lets a signed-in operator paste up to 200 Feishu record `编号` values, run a conservative automatic audit, and write the single-select field `是否分发` to `可分发` or `不可分发`.

Implementation files: `src/lib/distribution-check.ts`, `src/app/api/distribution-check/route.ts`, `src/app/distribution-check/page.tsx`, `src/lib/config.ts`, `src/lib/types.ts`, `src/app/page.tsx`, `scripts/harness/distribution_check.mjs`, and `scripts/harness/check.ps1`. The helper uses `lark-cli base +field-list`, `+record-search`, `+record-get`, and grouped `+record-batch-update`; it does not download/upload attachments. The API requires a workspace account before Feishu work.

Verification passed: `node scripts/harness/distribution_check.mjs`; `npx --no-install tsc --noEmit`; `npm run lint` with existing 3 warnings only; `npm run build`; full `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`; read-only `npm run db:diagnose -- --limit 5`; and `npm run local:restart`. The local production app is refreshed at `http://127.0.0.1:3001/`, and the tool is available at `http://127.0.0.1:3001/distribution-check`. No live Feishu writes were triggered during verification or restart smoke.

Important next entry point: perform a live validation only when intentionally testing with real `编号` values, because the page action writes `是否分发` in the target Feishu Base. Keep `.env.local`, Feishu tokens, and account credentials out of Harness docs and final answers.

Implementation completed on 2026-06-12 for real-time Feishu/Lark IM event task launch. New entry `npm run lark:events` (`scripts/lark-task-events.mjs`) consumes `im.message.receive_v1` through `lark-cli event consume --as bot`, waits for the `[event] ready` marker, filters to `LARK_TASK_CHAT_IDS`, normalizes event payloads into the same local `LarkTaskMessage` shape, and submits explicit `/flux` or command messages to the existing `POST /api/lark/tasks` path. It reuses `LARK_TASK_USER_MAP`, `LARK_TASK_API_TOKEN`, message-id idempotency in `lark_task_launches`, and `startSimpleRun(...)`; `--dry-run`, `--reply`, `--max-events`, `--timeout`, and `--self-test` are supported. Duplicate launches do not send repeated replies. Polling `npm run lark:tasks` remains available as a fallback.

Verification passed: `node scripts/lark-task-events.mjs --self-test`; `node scripts/lark-task-runner.mjs --self-test`; `node scripts/harness/lark_task_launcher_check.mjs`; bounded live event connectivity dry-run `npm run lark:events -- --dry-run --max-events 1 --timeout 15s` reached `[event] ready event_key=im.message.receive_v1` and exited after timeout with 0 events; `npx --no-install tsc --noEmit`; `npm run lint` with the existing 3 warnings only; `npm run build`; and full `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`. No FluxPost task, TikHub, OpenAI-compatible, image-provider, or Feishu publish production call was triggered by verification.

Important next entry point: use `npm run lark:events -- --reply` for real-time task launch after `http://127.0.0.1:3001/` is serving the current `/api/lark/tasks` implementation and `.env.local` has valid `LARK_TASK_*` values. If event consume has permission/subscription trouble, fall back to `npm run lark:tasks -- --reply` while diagnosing. Do not read or print `.env.local`; configure real `LARK_TASK_*` values out-of-band.

Implementation completed on 2026-06-12 for the user-requested content tag `提车记录`. Canonical content tags now include `提车记录`, and source tagging prompts define it as owner pickup/delivery/pickup-day records. Aliases such as `提车`, `提车作业`, `车主提车`, `新车交付`, and `交付记录` normalize to the canonical tag. These items remain in the content pool but are excluded from downstream production: `src/lib/production-plan.ts` marks them `observe_only` with `pickup_record_observe_only`, and `src/lib/simple-runs.ts` skips them before simple automatic production with a specific log reason. Manual content-tag edits now recalculate production plans in `src/lib/content-pool.ts`.

Implementation files: `src/lib/types.ts`, `src/lib/source-tagging.ts`, `src/lib/production-plan.ts`, `src/lib/simple-runs.ts`, `src/lib/content-pool.ts`, `scripts/harness/source_tagging_image_check.mjs`, and `scripts/harness/simple_crawl_media_policy_check.mjs`. Verification passed: `node scripts/harness/source_tagging_image_check.mjs`; `node scripts/harness/simple_crawl_media_policy_check.mjs`; `npx --no-install tsc --noEmit`; `npm run lint` with existing 3 warnings only; full `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`; read-only `npm run db:diagnose -- --limit 5 --logLimit 20`; and elevated `npm run local:restart` after the sandbox run hit known `spawn EPERM`. The local production app is refreshed at `http://127.0.0.1:3001/`. No live TikHub/OpenAI-compatible/image-provider/Feishu production call was triggered by verification or restart smoke.

Implementation completed on 2026-06-12 for the user-requested independent content review desk and batch Feishu write support. New page `src/app/review/page.tsx` exposes `/review` as a full-width review workspace with generated-post search/filtering, multi-select, large image preview, large title/body/image-prompt editing, manual save, AI prompt edit, single/batch approve, selected delete, and single/batch Feishu publish. It reuses existing owner-scoped APIs (`/api/production/posts`, `/api/review`, `/api/production/posts/batch`, `/api/publish/feishu`) and did not change Feishu queue core behavior. `src/app/page.tsx` now includes a `内容审查台` header link, and `src/app/globals.css` contains the review-page layout/styles.

Verification passed: `npx --no-install tsc --noEmit`; `npm run lint` with the existing 3 warnings only; `npm run build` with known Turbopack dynamic filesystem tracing warnings; read-only `npm run db:diagnose -- --limit 5` showing latest simple and Feishu publish queue rows completed and no blocked locks; full `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`; and `npm run local:restart`, which refreshed `http://127.0.0.1:3001/` and confirmed `/review` in the production route list. No live TikHub/OpenAI-compatible/image-provider/Feishu production call was triggered by verification or restart smoke.

Implementation and live repair completed on 2026-06-12 for the latest user-reported "鍐欏叆椋炰功澶辫触" / `timeout exceeded when trying to connect`. `src/lib/feishu-publish-queue.ts` now serializes generated-post local persistence before durable Feishu queue creation and after Feishu publish completion, with narrow transient PostgreSQL retry coverage for `40P01`, `40001`, deadlock messages, and `timeout exceeded when trying to connect`. `scripts/harness/feishu_publish_queue_check.mjs` guards against reintroducing `Promise.all(...persistOnePost...)` in those Feishu queue persistence paths.

The missed `simple-1781251944027` (`灏忛箯G6`) generated-post publish was replayed through the existing Feishu publish queue/publisher, without rerunning Xiaopeng BBS import, TikHub, OpenAI-compatible generation, image generation, or source import. Live job `feishu-publish-1781253700043-be472f8e` completed with `status=published`, wrote 17 Base records, uploaded all attachments (`attachmentFailureCount=0`), and sent the configured notification. Read-only verification confirmed 17/17 generated posts now have Feishu `recordId`, 17/17 are `status=published`, and 17/17 have `feishu.attachmentStatus=uploaded`. The run's top-level status is `partial` because historical errors from the failed attempt remain in the run, while the current publish result is successful.

Verification passed after the fix and live write: `node scripts/harness/feishu_publish_queue_check.mjs`; `npx --no-install tsc --noEmit`; read-only `npm run db:diagnose -- --run simple-1781251944027 --limit 8 --logLimit 100`; read-only generated-post/queue status query; and full `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1` with existing 3 lint warnings and known Turbopack tracing warnings only. The local production app at `http://127.0.0.1:3001/` was not restarted after this code change; run `npm run local:restart` when safe before relying on the running UI/API process to include this Feishu queue persistence fix.

Read-only diagnosis completed on 2026-06-12 for the latest user-reported "鍐欏叆椋炰功澶辫触" / `timeout exceeded when trying to connect`. The newest matching failed run is `simple-1781251944027` (`灏忛箯G6`), created `2026-06-12T08:12:24.026Z` and updated `2026-06-12T08:19:07.232Z`. It was a Xiaopeng BBS link-mode simple run with 20 thread ids. Link import succeeded for all 20 items. Source-import Feishu sync failed for all 20 source items at `2026-06-12T08:13:18.047Z` (`created=0`, `skippedDuplicate=0`, `failed=20`, first failed source item `xiaopeng_bbs-3776934`), but that step did not stop the workflow.

The run then generated and persisted 17 posts; 3 source items were skipped for having no images/downloaded images/video frames. The 17 generated posts are in `generated_posts` as `approved`, owned by `acct-1780914293402-dc1c0796`, with no Feishu `recordId`. There is no `feishu_publish_queue` row for `source_run_id=simple-1781251944027`, so the final publish failure occurred before durable Feishu publishing was enqueued and before a final generated-post Base write ran.

Current conclusion: the final `timeout exceeded when trying to connect` is most likely local PostgreSQL connection-pool exhaustion/contention during publish enqueue preparation, not a confirmed Feishu Base write failure, Base permission/field issue, PostgreSQL lock blocker, or current Feishu endpoint outage. Evidence: no publish queue job exists for the run; `npm run db:diagnose -- --run simple-1781251944027 --limit 20 --logLimit 220` showed no blocked locks; `lark-cli config show`, `lark-cli doctor --offline`, and live `lark-cli doctor` passed after the failure, with bot identity ready and Feishu endpoints reachable. Relevant code risk: `src/lib/feishu-publish-queue.ts` still does `Promise.all(posts.map(persistOnePost))` before `saveFeishuPublishJobToDb(...)`; each item calls `savePost`, `saveGeneratedPost`, and `markSourceRewritten`, creating a burst of PostgreSQL work before a durable publish job exists. Do not treat the approved generated posts as lost.

Recommended next task: fix/guard the Feishu publish enqueue preparation so approved-post persistence before queue creation is serialized or otherwise bounded like the earlier simple-run publish-prep fix, and ensure source-import Feishu side-effect failures cannot obscure final generated-post publish state. After a code fix, verify with focused static/runtime checks plus full Harness baseline; do not replay this historical run into Feishu without explicitly confirming whether the existing 17 approved posts should now be enqueued for publish. `lark-cli doctor` also reported update notice current `1.0.45`, latest `1.0.52`.

Implementation completed on 2026-06-12 for Feishu/Lark IM launch of Xiaopeng BBS source links and thread ids. `src/lib/lark-task-launcher.ts` now recognizes Xiaopeng BBS aliases in task messages: `/flux xiaopeng 3776077`, `/flux xpeng 3776077`, `/flux xiaopeng_bbs 3776077`, `/flux links xiaopeng 3776077`, `/flux <Chinese Xiaopeng Community alias> 3776077`, and full URLs such as `/flux xiaopeng https://bbs.xiaopeng.com/thread/3776077?tidType=1`. These parse as `sourceMode="links"`, `links=[...]`, and `linkPlatform="xiaopeng_bbs"`, then reuse the existing simple-run link pipeline and `src/lib/xiaopeng-bbs.ts` importer. Xiaopeng BBS remains a source-link/ID platform only, not a TikHub keyword crawl platform.

Verification passed: `node scripts/harness/lark_task_launcher_check.mjs`; `node scripts/harness/xiaopeng_bbs_import_check.mjs`; `node scripts/harness/simple_link_run_check.mjs`; `node scripts/lark-task-runner.mjs --self-test`; `npx --no-install tsc --noEmit`; `npm run lint` with the existing 3 warnings only; `npm run build`; and full `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`. Default verification did not trigger live Feishu polling, TikHub, OpenAI-compatible, image-provider, or Feishu production calls.

Important next entry point: do not assume the running local production app at `http://127.0.0.1:3001/` has this IM parser update yet. `npm run local:restart` was intentionally skipped because read-only `npm run db:diagnose -- --limit 5` showed real Feishu publish job `feishu-publish-1781249473744-f0b26f80` for `simple-1781249115291` still `running` with a valid worker lock. After that job finishes, run `npm run local:restart` before a live Feishu IM test of Xiaopeng BBS commands. Do not read or print `.env.local`; configure real `LARK_TASK_*` values out-of-band.

Implementation completed on 2026-06-12 for the user-requested Xiaopeng BBS source-link/ID import. Users can now use link-import mode with platform `灏忛箯绀惧尯` and paste either pure thread ids such as `3776077` or full URLs like `https://bbs.xiaopeng.com/thread/3776077?tidType=1`. Full Xiaopeng BBS thread URLs also auto-detect in link mode. Pure numeric ids require selecting `灏忛箯绀惧尯` so they are not confused with other numeric platform ids.

Behavior and files: `src/lib/xiaopeng-bbs.ts` fetches public Xiaopeng BBS thread HTML, reads the Next.js `pageData` payload, and normalizes author, title/topic, body, attachment images, publish time, metrics, source URL, and raw payload into `NormalizedSourceItem` with `platform="xiaopeng_bbs"`. `src/lib/source-link-import.ts` dispatches Xiaopeng BBS source links to this local importer instead of TikHub; `src/lib/types.ts` separates `SourceLinkPlatform` from `CrawlPlatform`; `src/lib/simple-runs.ts`, `src/app/api/crawl/links/route.ts`, `src/app/api/simple/runs/route.ts`, and `src/app/page.tsx` carry the platform through advanced and simple link-import flows. `src/lib/source-import-feishu.ts` and `src/lib/feishu-cli.ts` label/canonicalize Xiaopeng BBS records for Feishu paths. Xiaopeng BBS is not a keyword crawl platform and should not be added to TikHub crawl controls.

Verification passed: `node scripts/harness/xiaopeng_bbs_import_check.mjs`; `node scripts/harness/link_import_check.mjs`; `node scripts/harness/simple_link_run_check.mjs`; `node scripts/harness/feishu_content_import_check.mjs`; `npx --no-install tsc --noEmit`; `npm run lint` with the existing 3 warnings only; `npm run build`; and full `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`. Baseline emitted only known Turbopack dynamic filesystem tracing warnings and the expected existing lint warnings. No live TikHub/OpenAI-compatible/image-provider/Feishu production call was triggered by verification.

Important next entry point: do not assume the running local production app at `http://127.0.0.1:3001/` has this implementation yet. `npm run local:restart` was intentionally skipped because read-only `npm run db:diagnose -- --limit 5` showed real simple run `simple-1781245493443` (`灏忛箯P7+`) still running with a valid simple-run queue lock. After active simple work finishes, run `npm run local:restart` before a live UI test of Xiaopeng BBS import.

Implementation completed on 2026-06-12 for V1 Feishu/Lark conversation task launch. New files/entry points: `src/lib/lark-task-launcher.ts`, `src/app/api/lark/tasks/route.ts`, `scripts/lark-task-runner.mjs`, and `scripts/harness/lark_task_launcher_check.mjs`. Updated files include `src/lib/config.ts`, `src/lib/database.ts`, `src/lib/types.ts`, `db/migrations/001_initial_postgres.sql`, `package.json`, `scripts/harness/check.ps1`, `.env.example`, and README.

Behavior: `npm run lark:tasks` polls allow-listed Feishu/Lark chats with `lark-cli im +chat-messages-list --as bot`, recognizes explicit `/flux` or `鍙戠` commands, and submits matched messages to local `POST /api/lark/tasks`. The route requires `LARK_TASK_API_TOKEN`, enforces `LARK_TASK_CHAT_IDS`, maps Feishu sender open ids to existing workspace accounts through `LARK_TASK_USER_MAP`, persists `lark_task_launches` idempotency by `message_id`, and enqueues through `startSimpleRun(...)`. Supported commands launch keyword, source-link, or Feishu task-number simple runs. Counts above `LARK_TASK_CONFIRM_ABOVE` require `confirm=yes`. Replies are opt-in with runner flag `--reply`; `--dry-run` prints matched messages without launching tasks; `--self-test` does not call Feishu.

Verification passed: `node scripts/lark-task-runner.mjs --self-test`; `node scripts/harness/lark_task_launcher_check.mjs`; `node scripts/harness/postgres_schema_check.mjs`; `npx --no-install tsc --noEmit`; `npm run lint` with existing 3 warnings only; `npm run build`; and full `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`. Default verification did not trigger real TikHub/OpenAI/image-provider/Feishu production calls. A deliberate fake-chat `--dry-run` reached real `lark-cli` and failed with Feishu `invalid container_id`; its output also reported a lark-cli update notice from `1.0.45` to `1.0.52`.

Important next entry point: do not assume the running local production app at `http://127.0.0.1:3001/` has this API yet. `npm run local:restart` was intentionally skipped because `npm run db:diagnose -- --limit 5` showed two real simple runs still running: `simple-1781245493443` and `simple-1781245420005`. After active work finishes, run `npm run local:restart` before a live Feishu IM test. Do not read or print `.env.local`; configure real `LARK_TASK_*` values out-of-band.

Read-only diagnosis completed on 2026-06-12 for the user's newest "15 links, only one succeeded" report. The matching run is `simple-1781237983372` (`灏忛箯GX`, created `2026-06-12T04:19:43.372Z`): it recorded 19 link results, imported 15 links successfully, had 4 link-resolution failures, generated/published 1 post through Feishu job `feishu-publish-1781238367255-9cbc407c`, and ended `partial`. The failure point for the other imported items was image generation, not link import, TikHub, source safety, text generation, Feishu queue, or PostgreSQL locks. Recent logs and run errors show repeated OpenAI Images failures: `403 insufficient_user_quota` (`user quota is not enough`) and some `405` image route failures. `/api/config` reports `imageModel=gpt-image-2`, `imageProvider=images`, `openaiImageBaseUrl=https://lingsuan.top/v1`, and backup `openaiImageBackupBaseUrl=https://run.v3.cm/v1`. No runtime data, provider config, Feishu records, or generated media were mutated during diagnosis.

Adjacent context: immediately previous run `simple-1781237903873` imported 2/2 links and published 2/2 posts successfully. Earlier newest failed simple runs (`simple-1781236550870`, `simple-1781236642940`, `simple-1781236913082`, `simple-1781237006354`, `simple-1781237041425`, and `simple-1781237072175`) all targeted `灏忛箯GX`, used Douyin source links, imported source items successfully, then failed with 0 generated posts before Feishu publish because the text-model route returned `404 No static resource codex-pro/v1/chat/completions` or HTML `<!doctype...` bodies that failed JSON parsing.

Verification and documentation completed for the diagnosis: `docs/harness/progress.md` and `docs/harness/handoff.md` were updated. A pre-existing `.env.example` drift was corrected from `COMFYUI_KLEIN_ENABLED=true` back to the documented/default-safe `COMFYUI_KLEIN_ENABLED=false`; `.env.local` and runtime provider configuration were not read or changed. Full `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1` passed afterward with the existing 3 ESLint warnings and known Turbopack dynamic filesystem tracing warnings only.

Follow-up image-generation diagnosis on 2026-06-12 clarified why recent user-started tasks can show both success and failure. Later runs `simple-1781240962059` and `simple-1781242282072` completed and published, and generated-post rows confirm real `/generated/...png` image outputs for their posts. During `simple-1781242282072`, recent `openai/image` logs show 18 successful `Images 鍥剧墖妯″瀷瀹屾垚` entries, 3 primary-route `524` retries, then one primary `https://lingsuan.top/v1/images/edits` timeout after 47s, failover to backup, and backup `images/edits` failure with `403 insufficient_user_quota`. This means the primary image route is intermittently capable of returning successful edit outputs but is unstable under bursts/timeouts, while the backup route is currently not a useful rescue path because it has insufficient quota for `/images/edits`. Some successful published posts may also include `Keep source image` slots, which intentionally do not call the image model, or source-image fallback slots from earlier recoverable failures; inspect `generated_posts.imageUrls` and `execution_logs` before interpreting a published post as a full image-model success.

Read-only diagnosis completed on 2026-06-12 for the user's latest failed simple link-mode tasks. The newest six simple runs were `灏忛箯GX` Douyin link-mode runs and all failed before Feishu generated-post publish with 0 generated posts. Latest run `simple-1781237072175` successfully resolved/imported 2/2 Douyin links, kept both source items, and synced 2 Feishu source-import records, but both production items failed with `Unexpected token '<', "<!doctype "... is not valid JSON`; publish was skipped because no generated posts existed.

Confirmed root cause: OpenAI-compatible text model route/config failure. `/api/config` reports `textModel=gpt-5.5` and `openaiTextBaseUrl=https://lingsuan.top`. Recent logs show safety, tagging, and generation calls failing against `/codex-pro/v1/chat/completions` with `404 No static resource codex-pro/v1/chat/completions`, then later returning HTML `<!doctype...` bodies that fail JSON parsing. This is not a TikHub, PostgreSQL lock, Feishu publish, image-provider, or ComfyUI failure. `npm run db:diagnose -- --limit 10 --logLimit 80` showed no active Feishu queue and no blocked locks. No runtime data or provider config was changed during diagnosis.

Recommended next entry point: fix text-model env routing/model (`OPENAI_TEXT_BASE_URL`/`OPENAI_BASE_URL`, `OPENAI_TEXT_ENDPOINT`, `OPENAI_TEXT_MODEL`) to a working OpenAI-compatible endpoint, restart `http://127.0.0.1:3001` with `npm run local:restart`, then retry a small link-mode run. If the relay only supports chat completions, keep `OPENAI_TEXT_ENDPOINT=chat` and include the relay's required API prefix in the base URL.

Implementation completed on 2026-06-12 for the user-requested environment switch around the previously imported local ComfyUI workflow. Local ComfyUI Klein routing is now opt-in: `COMFYUI_KLEIN_ENABLED=false` is the default, and with that setting car-exterior, `杞﹀瀷缇庡浘`, and people-with-car selected source-image tasks use the existing GPT-Image-2/OpenAI Images API path. Only `COMFYUI_KLEIN_ENABLED=true` plus either `COMFYUI_KLEIN_WORKFLOW_API_JSON`/`COMFYUI_KLEIN_WORKFLOW_JSON` or `COMFYUI_KLEIN_WORKFLOW_PATH` routes those tasks to local ComfyUI.

Implementation files: `src/lib/config.ts`, `src/lib/creation-controls.ts`, `src/lib/comfyui-klein.ts`, `src/lib/image-generation.ts`, `src/lib/simple-runs.ts`, `src/lib/types.ts`, `src/app/page.tsx`, `.env.example`, `README.md`, and `scripts/harness/comfyui_klein_check.mjs`. Verification passed: `node scripts/harness/comfyui_klein_check.mjs`; `node scripts/harness/image_task_fallback_check.mjs`; `npx --no-install tsc --noEmit`; `npm run lint` with existing 3 warnings only; `npm run build`; full `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`; and read-only `npm run db:diagnose -- --limit 5`, which showed no active simple queue, no active Feishu publish queue, and no blocked locks. A fresh read-only queue check also showed no active simple/Feishu work before `npm run local:restart`; the local production app is refreshed at `http://127.0.0.1:3001/`. No live TikHub/OpenAI-compatible/image-provider/ComfyUI/Feishu production call was triggered by verification or restart smoke.

Implementation completed on 2026-06-11 for the user-requested visual tag expansion in image processing. New allowed visual tags are `APP` and `杞﹀瀷缇庡浘`. `APP` is the canonical stored label for application interfaces, including phone app screenshots and vehicle infotainment/instrument UI; aliases such as `搴旂敤鐣岄潰` and `杞︽満鐣岄潰` normalize to `APP`. `APP` and `鍐呴グ绌洪棿` keep the original image and do not call image models. `杞﹀瀷缇庡浘` means pure vehicle exterior beauty shots without people and reuses the existing `姹借溅澶栬` car-exterior ComfyUI Klein route. `浜鸿溅缇庡浘` remains for exterior car images with a visible person participating in the scene.

Implementation files: `src/lib/types.ts`, `src/lib/source-tagging.ts`, `src/lib/creation-controls.ts`, `src/lib/simple-runs.ts`, `src/app/page.tsx`, `scripts/harness/source_tagging_image_check.mjs`, and `scripts/harness/comfyui_klein_check.mjs`. Verification passed: `node scripts/harness/source_tagging_image_check.mjs`; `node scripts/harness/comfyui_klein_check.mjs`; `npx --no-install tsc --noEmit`; `npm run lint` with existing 3 warnings only; `npm run build`; full `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`; read-only `npm run db:diagnose -- --limit 5`; and `npm run local:restart`. The local production app is refreshed at `http://127.0.0.1:3001/`.

Diagnosis and implementation completed on 2026-06-11 for the previous 灏忛箯P7+ simple link-mode task that surfaced `妫€娴嬪埌姝婚攣`. Read-only diagnostics showed no active PostgreSQL blocked locks and no active simple/Feishu queue locks. The failing run was `simple-1781160663254`: it imported, tagged, and generated 10 Douyin drafts, then failed in local publish preparation before Feishu enqueue with publish message `妫€娴嬪埌姝婚攣`; no Feishu publish job was created. The sibling 灏忛箯P7+ run `simple-1781160613200` published 9 Xiaohongshu posts successfully.

The confirmed root cause was application-level PostgreSQL write contention in local runtime persistence, not TikHub, OpenAI/image, Feishu CLI, or durable queue lease locking. `src/lib/simple-runs.ts` no longer fans out approved-post persistence with `Promise.all(approvedPosts.map(...))`; approved posts are saved sequentially through `persistApprovedPostsForSimplePublish(...)` with short retries for transient PostgreSQL deadlock/serialization errors. `src/lib/activity-log.ts` now appends execution logs through `appendExecutionLogToDb(...)` in `src/lib/database.ts`, trimming old rows instead of reading and rewriting the whole `execution_logs` table on every log entry.

Verification passed: `node scripts/harness/execution_log_append_check.mjs`; `node scripts/harness/simple_run_persistence_check.mjs`; `npx --no-install tsc --noEmit`; `npm run lint` with existing 3 warnings only; full `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`; read-only `npm run db:diagnose -- --limit 8` showing no active queued/running simple or Feishu jobs and no blocked locks; and `npm run local:restart` refreshed `http://127.0.0.1:3001/`. No live TikHub/OpenAI-compatible/image-provider/ComfyUI/Feishu production call was triggered by verification. During that fix verification, historical run `simple-1781160663254` was not replayed or mutated.

Operational Feishu result write completed on 2026-06-11 for failed Xiaopeng P7+ run `simple-1781160663254`, per the user's explicit request. The operation reused the existing Feishu publish queue/publisher for the 10 already-generated Douyin posts and did not rerun TikHub, OpenAI-compatible generation, image generation, or source import. Feishu job `feishu-publish-1781165356373-367aa0bb` completed with `status=published`, wrote 10 Base records, uploaded 47 attachment files, and sent the configured notification. Generated post rows now have 10/10 `status=published`, 10/10 Feishu `recordId`, and 10/10 `feishu.attachmentStatus=uploaded`. The run's top-level `status` remains `partial` only because the historical `errors` array still contains `妫€娴嬪埌姝婚攣`; the `publish.status` object is `published`.

Verification for the result write passed after the live Feishu operation: read-only `npm run db:diagnose -- --run simple-1781160663254 --limit 10 --logLimit 20`; read-only generated-post status query; and full `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1` with existing lint warnings and known Turbopack dynamic public-path warnings only. No code was changed.

Operational Feishu source-import backfill completed on 2026-06-11 for the user's 2026-06-10 link-mode simple runs. Read-only comparison found 93 imported source-link candidates from 18 link-mode runs: 60 already had complete Feishu source records, and 33 had existing Feishu rows with source links but empty content fields/attachments. No new Feishu records were created. The 33 existing rows were updated with source URL/title/body/platform and had local attachments appended: 165 image/frame files and 12 video files. A same-source empty duplicate was left untouched where a complete same-source record already existed.

Verification for the backfill passed after the live Feishu write: the write script verified all 33 target rows had content and required attachments; read-only `npm run db:diagnose -- --limit 5` showed no active simple or Feishu queue items; full `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1` passed with the existing 3 lint warnings and known Turbopack dynamic public-path warnings. The operation used `lark-cli base +record-batch-update` and `+record-upload-attachment` with bot identity against the documented source-import Base/table. No code was changed.

Implementation completed on 2026-06-11 for preventing Douyin body descriptions from being written as Feishu source-import titles; this supersedes the earlier diagnosis notes below. `src/lib/tikhub.ts` now keeps Douyin `desc`/`content`/`text_raw` in `contentText` only; `NormalizedSourceItem.title` is populated only by explicit Douyin title fields. `src/lib/source-import-feishu.ts` writes the source-import `鏍囬` field from `item.title` only and allows blank titles instead of falling back to body text. `src/lib/source-link-import.ts` no longer labels body previews as result `title`. Per user request, no historical Feishu/source records were repaired.

Harness guards were added in `scripts/harness/link_import_check.mjs` and `scripts/harness/source_import_feishu_check.mjs` for desc-only Douyin fixtures and source-import no-body-title fallback. Verification passed: `node scripts/harness/link_import_check.mjs`; `node scripts/harness/source_import_feishu_check.mjs`; `npx --no-install tsc --noEmit`; `npm run lint` with the existing 3 warnings only; and full `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`. No live TikHub/OpenAI-compatible/image-provider/ComfyUI/Feishu production call was triggered.

Read-only diagnosis completed on 2026-06-11 for Douyin source-link imports writing body descriptions into the Feishu source-import `title` field. The deterministic source-table path is: `src/lib/tikhub.ts` maps Douyin `desc`/`content`/`text_raw` into `NormalizedSourceItem.title` and `contentText`; `src/lib/source-import-feishu.ts` writes source-link records with `resolveSourceTitle(item)`, which currently falls back to `compactText(item.contentText, 80)` when title is absent. `src/lib/feishu-cli.ts` generated-post publishing maps `title` to `post.title` and `body` to `post.body`, so generated-post Feishu field order is not the direct cause, though the same source-title pollution can affect generation prompts through `鍘熸爣棰榒.

Recommended next implementation entry points: add platform-aware source-title extraction in `src/lib/tikhub.ts` so Douyin `desc` only populates `contentText`; change `resolveSourceTitle(...)` in `src/lib/source-import-feishu.ts` to never use body text as a title (blank or source-id placeholder only if needed); change `resultFromItem(...)` in `src/lib/source-link-import.ts` so UI/import summaries do not label `contentText` previews as `title`; add Harness guards to `scripts/harness/link_import_check.mjs` and `scripts/harness/source_import_feishu_check.mjs`; optionally repair historical source/import records where Douyin `title` equals or is a prefix of `contentText`. Verification already passed for the diagnosis: relevant static checks, a mocked desc-only Douyin normalization fixture, and full `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`.

Implementation completed on 2026-06-10 for the user-requested generated-title hard maximum. `src/lib/title-guard.ts` now centralizes the generated-title rule with a global 10-20 visible-character range and randomized profiles `10-13`, `14-17`, and `18-20`; the prompt explicitly states the iron rule that final `title` must never exceed 20 visible characters/Chinese characters. `src/lib/openai.ts` uses the shared guard in the primary generation prompt, title-only repair prompt, validator, local fallback, no-key demo fallback, and AI review-edit path. `src/lib/generated-posts.ts` clamps every saved generated-post title through `clampGeneratedTitleMax(...)`, so save paths cannot persist generated-post titles above 20 visible characters.

`scripts/harness/title_prompt_guard_check.mjs` now guards the hard `<=20` maximum, updated profiles, prompt-level iron rule, profile-aware repair/fallback, demo fallback clamp, AI review-edit clamp, generated-post persistence clamp, and absence of the old `21-26` range. Verification passed on 2026-06-10: `node scripts/harness/title_prompt_guard_check.mjs`; `npx --no-install tsc --noEmit`; `npm run lint` with the existing 3 warnings only; full `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`; read-only `npm run db:diagnose -- --limit 5` showing no active queued/running simple or Feishu jobs; and `npm run local:restart` with local HTTP smoke passing on `http://127.0.0.1:3001/`. No live TikHub/OpenAI-compatible/image-provider/ComfyUI/Feishu production call was triggered by verification.

Implementation completed on 2026-06-10 for the user-requested Feishu task-number source channel in simple mode. Users can paste Feishu `浠诲姟缂栧彿` values; `src/lib/feishu-content-import.ts` searches the configured Feishu Base/table by exact `浠诲姟缂栧彿`, reads `鍔ㄦ€佹爣棰榒, `鍔ㄦ€佹鏂嘸, `鍔ㄦ€佺礌鏉恅, and `杞﹀瀷`, downloads `鍔ㄦ€佺礌鏉恅 attachments through `lark-cli base +record-download-attachment`, stores imported materials under `public/media/crawl/feishu`, and returns normalized `platform="feishu"` source items. `杞﹀瀷` maps to the app's keyword/content-pool project so imported items are automatically grouped into the corresponding vehicle library.

Simple-run wiring now supports `sourceMode="feishu"` plus `feishuTaskNumbers` in `src/app/api/simple/runs/route.ts`, `src/lib/simple-runs.ts`, `src/lib/types.ts`, and `src/app/page.tsx`. Feishu-mode runs skip TikHub keyword search and top-up, persist import results on `SimpleRun.feishuResults`, reuse source safety, AI tagging, production, image generation, and queued Feishu publishing, and publish generated output through the existing generated-post Feishu fields. Feishu is represented as a source platform for imported items, but TikHub crawl/link APIs still use only crawl platforms.

Configuration added: `FEISHU_CONTENT_IMPORT_BASE_TOKEN`, `FEISHU_CONTENT_IMPORT_TABLE_ID`, and `FEISHU_CONTENT_IMPORT_FIELD_MAP`. The import Base/table default to `FEISHU_BITABLE_APP_TOKEN` and `FEISHU_BITABLE_TABLE_ID` when the import-specific values are omitted. Default field map is `浠诲姟缂栧彿`, `鍔ㄦ€佹爣棰榒, `鍔ㄦ€佹鏂嘸, `鍔ㄦ€佺礌鏉恅, and `杞﹀瀷`. Do not read or print `.env.local` while checking real local configuration.

Verification passed on 2026-06-10: `node scripts/harness/feishu_content_import_check.mjs`; `node scripts/harness/simple_link_run_check.mjs`; `node scripts/harness/simple_queue_check.mjs`; `node scripts/harness/workspace_accounts_check.mjs`; `npx --no-install tsc --noEmit`; `npm run lint` with the existing 3 warnings only; `npm run build` with known Turbopack dynamic public-path warnings; and full `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`. Default verification did not call real TikHub, OpenAI-compatible services, image providers, ComfyUI, or Feishu production APIs.

Read-only `npm run db:diagnose -- --limit 5` on 2026-06-10 showed no active queued/running simple or Feishu queue rows. `npm run local:restart` then rebuilt and refreshed `http://127.0.0.1:3001/`; local HTTP smoke passed. The local production app now serves the Feishu task-number import implementation.

Previous task context: implementation also completed on 2026-06-10 for the user-requested switch from GPT-Image-2 to local ComfyUI Klein for the two image strategies most likely to hit image safety review: car exterior/background replacement and people-with-car/background+person replacement. `src/lib/creation-controls.ts` routes `carExterior` and `peopleWithCar` selected source-image tasks to `provider="comfyui_klein"`; text-image and other tasks remain on the existing OpenAI Images API path, and interior tasks still keep the source image.

The local ComfyUI integration lives in `src/lib/comfyui-klein.ts`. It reads `COMFYUI_KLEIN_WORKFLOW_PATH` on each task, supports both ComfyUI UI workflow JSON and API prompt JSON, preserves `extra.ue_links` for Anything Everywhere hidden links, uploads the selected reference image to ComfyUI `/upload/image`, submits `/prompt`, polls `/history/{prompt_id}`, downloads outputs through `/view`, and stores final images in `public/generated/klein`. Environment knobs include `COMFYUI_BASE_URL`, node ids for prompt/image/KSampler/save nodes, upload subfolder, timeout/poll interval, seed/random-seed, KSampler steps/cfg/sampler/scheduler/denoise, and `COMFYUI_KLEIN_FAILURE_POLICY`. The confirmed local workflow file is `C:\Users\Administrator\Downloads\FluxPost Klein 鍥剧墖閲嶆瀯.json`; do not read or print `.env.local` while checking this.

Concurrency boundary: user/simple/batch work can still run concurrently, but local Klein image execution is serialized by the dedicated `localImage` pool in `src/lib/concurrency.ts`; `WORKER_LOCAL_IMAGE_CONCURRENCY` defaults to `1` and is hard-capped at `1`. `src/lib/database.ts` and `db/migrations/001_initial_postgres.sql` now include `image_generation_queue` for local image job observability. This is not yet a replay worker; it records queued/running/completed/failed state around the in-process Klein call. With `COMFYUI_KLEIN_FAILURE_POLICY=fallback_source`, a failed Klein slot uses the original source image so a post can continue.

New verification entry points: `node scripts/harness/comfyui_klein_check.mjs`, plus the updated `scripts/harness/concurrency_check.mjs`, `scripts/harness/postgres_schema_check.mjs`, and full `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`. Default verification must not call live ComfyUI generation; use a live Klein smoke only if the user explicitly wants to generate a real local image.

Verification passed on 2026-06-10: focused Klein/schema/concurrency/image checks, TypeScript noEmit, lint with existing 3 warnings, Next build with known Turbopack dynamic public-path warnings, and full Harness baseline. `npm run local:restart` was not run because `npm run db:diagnose -- --limit 5` showed real run `simple-1781083279049` still `running` with a valid simple-run queue lock. After that run completes, run `npm run local:restart` to refresh `http://127.0.0.1:3001/` with the ComfyUI Klein implementation.

Read-only diagnosis completed on 2026-06-10 for recent user-started batch/simple tasks having partial failures. Use `npm run db:diagnose` and read-only PostgreSQL queries through `FLUXPOST_DIAG_DATABASE_URL` for follow-up; do not read or print `.env.local` secrets. No runtime data, generated media, cached media, or production service state was mutated during the diagnosis.

Findings: recent failures are not from advanced `batch_jobs` (only historical completed jobs exist). They are in simple-run link/keyword batches. There are three distinct causes. First, the image provider path is unstable under recent load: primary `right.codes` `/images/edits` calls time out, backup `run.v3.cm` `/images/edits` now returns `403 insufficient_user_quota`, and generic transport `fetch failed` can still become a hard per-source image failure. Example: `simple-1781073675994` imported/tagged 10 Douyin links and published 9 posts, but source `douyin-7517899084142972220` failed production with `fetch failed` despite complete local media and 5 extracted frames. Second, some user-provided Douyin links are search modal URLs such as `https://www.douyin.com/search/...&modal_id=...`; current parsing passes the whole search URL to the TikHub share-url endpoint, which returns `400`, so fewer links become candidates. Example: `simple-1781076950584` had 5 links but 3 failed this way and only 2 posts were produced. Third, video-like sources without extracted frames are intentionally skipped by simple automatic production; example `simple-1781076881003` published 7/10 because three Xiaohongshu video items had only cover images, no local video, and `videoFrames=0`.

The live run observed during diagnosis, `simple-1781079315232`, later completed as `partial`: it imported/tagged 4 Douyin links, production completed 1 post and failed 3, then Feishu publish completed for the 1 successful post. The failed production items were `All image tasks failed` with repeated Images API `403 insufficient_user_quota`, confirming the image backup quota/config problem as the newest direct cause. A final read-only queue check at `2026-06-10T08:22:27Z` showed no active queued/running simple or Feishu queue rows.

Likely next implementation entry points if asked to fix: update `src/lib/source-link-import.ts`/`src/lib/tikhub.ts` so Douyin search modal URLs extract `modal_id`/content ids before resolution; update `src/lib/image-generation.ts` so generic temporary transport errors such as `fetch failed` are eligible for the same source-image fallback policy as timeouts/gateway/5xx where appropriate; and consider an operator-facing status distinction for intentional no-frame video skips versus true production failures. Recheck image backup route quota/config separately before large batches.

Verification after the diagnosis documentation update passed: full `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1` with existing lint warnings and known Turbopack tracing warnings only.

Configuration completed on 2026-06-10 for reusable read-only PostgreSQL diagnostics. Dedicated local role `fluxpost_reader` now exists for `fluxpost_studio`; its secret connection string is persisted only as Windows user environment variable `FLUXPOST_DIAG_DATABASE_URL`. The role is read-only by default, has short statement/lock/idle timeouts, can read app runtime tables plus safe account/session views under `diagnostics`, and can inspect PostgreSQL stats/settings for session and lock diagnosis. `log_lock_waits=on`, `deadlock_timeout=1s`, and `log_min_error_statement=error` were applied through PostgreSQL config reload without restarting the service.

Project entry point: run `npm run db:diagnose` from the repo root. Added files: `scripts/local/postgres-diagnose.ps1` and `scripts/local/postgres-diagnose.mjs`; `package.json` now exposes `db:diagnose`. Verification passed: `npm run db:diagnose -- --limit 5`; a write probe was blocked by read-only transaction; `pg_hba.conf` was restored after temporary local admin access and no temporary trust rule remained; `npm run lint` with existing warnings only; `npx --no-install tsc --noEmit`; `npm run build` with known Turbopack tracing warnings only; and full `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`. Latest diagnostic output showed no active queued/running simple or Feishu queue rows.

Implementation completed on 2026-06-10 for batch-aware Feishu CLI IM publish notifications. `src/lib/feishu-cli.ts` now accepts notification context from the Feishu publish queue and builds notifications as task/batch summaries rather than labeling the first generated post title as the whole notification title. Batch messages show task/source/job context, record/material counts, and up to three `鍐呭绀轰緥`; single-post messages use `鍐呭`. Attachment-upload failures now attempt a `鍐欏叆椋炰功閮ㄥ垎瀹屾垚` warning notification when IM recipients are configured.

`src/lib/feishu-publish-queue.ts` passes job id, source, source run id, owner id, and available owner display name into `publishPostsToFeishu(...)`. `scripts/harness/feishu_publish_queue_check.mjs` guards the batch-summary notification behavior and prevents reintroducing `posts[0]` as the batch title. Verification passed: `node scripts/harness/feishu_publish_queue_check.mjs`; `npx --no-install tsc --noEmit`; `npm run lint` with existing warnings only; `npm run build` with known Turbopack tracing warnings only; and full `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`. No live TikHub/OpenAI-compatible/image-provider/Feishu call was triggered.

The local `http://127.0.0.1:3001/` production server was not restarted after this change because a read-only PostgreSQL queue check showed `simple-1781061211649` still `running` in both `simple_runs` and `simple_run_queue`. Next entry point: after that run finishes, run `npm run local:restart` so port 3001 serves the new Feishu notification implementation.

Historical note, superseded by the 2026-06-10 hard `<=20` title rule above: implementation completed on 2026-06-09 for making generated content title lengths more random. At that time `src/lib/openai.ts` set the global generated-title range to 10-26 visible characters and randomly chose one profile for each draft: short 10-14, medium 15-20, or long 21-26. That old range is no longer the current behavior.

The title validator, model repair path, and local fallback now validate/fit against the selected random profile. `scripts/harness/title_prompt_guard_check.mjs` now protects the randomized profile behavior instead of the old 12-18 rule. Verification passed: `node scripts/harness/title_prompt_guard_check.mjs`; `npx --no-install tsc --noEmit`; `npm run lint` with existing warnings only; full `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`; read-only PostgreSQL queue status check showing no queued/running simple or Feishu jobs; and `npm run local:restart` after the sandbox run hit the known `spawn EPERM` build limitation. The local app is refreshed at `http://127.0.0.1:3001/`.

Read-only diagnosis completed on 2026-06-09 for the user report that generated content titles are now mostly around 12 Chinese characters. The cause is the intentional generated-title guard in `src/lib/openai.ts`: titles are prompted and validated against a 12-18 visible-character range; invalid model titles are repaired with a title-only prompt; if repair still fails, `buildLocalTitleFallback(...)` and `fitTitleLength(...)` locally build a contextual title and force it into the same range. Because the model usually satisfies hard numeric ranges at the minimum acceptable value, and local fallback takes the first valid candidate, current titles naturally cluster near 12 characters.

`scripts/harness/title_prompt_guard_check.mjs` is wired into `scripts/harness/check.ps1` and protects this title-length behavior. To change it, update both the title constants/prompt/repair behavior in `src/lib/openai.ts` and the title guard check. No business code, runtime data, provider calls, or `.env.local` secret values were changed during this diagnosis. Verification after this documentation update passed: full `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1` with existing lint/Turbopack warnings only.

Read-only diagnosis completed on 2026-06-09 for the user report that the GPT-Image-2 backup API route did not take effect. The running local app at `http://127.0.0.1:3001/api/config` reports `imageProvider=images`, `imageModel=gpt-image-2`, `openaiImageBackupConfigured=true`, primary image base URL `https://right.codes/draw/v1`, and backup image base URL `https://run.v3.cm`. Recent PostgreSQL `execution_logs` under `scope=openai/image` show the backup route did take effect: primary `/images/edits` calls timed out, then `Images API failover to backup` entries were recorded. The subsequent backup requests failed with `404 Invalid URL (POST /images/edits)` on route `backup` and `endpointPath=images/edits`. Root cause: the configured backup route is not accepting the standard OpenAI Images API edit path currently used for reference-image tasks, either because the base URL is missing its required OpenAI-compatible prefix such as `/v1` or because that relay does not support multipart `/images/edits`.

No code, runtime data, provider calls, or `.env.local` secret values were changed during this diagnosis. Verification after the documentation update passed: full `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1` after the first sandbox attempt hit the known Next build `spawn EPERM` limitation.

Implementation completed on 2026-06-09 for the updated TikHub Xiaohongshu App V2 search documentation at `https://docs.tikhub.io/420136398e0`. `src/lib/tikhub.ts` now preserves the documented App V2 sort values `comment_descending`, `collect_descending`, and `english_preferred`, while retaining `general`, `time_descending`, and `popularity_descending`; `noteType=3` now maps to `鐩存挱绗旇`.

`src/app/page.tsx` now points the Xiaohongshu platform document link to `https://docs.tikhub.io/420136398e0` and exposes the new comment-sort, collect-sort, English-preferred, and live-note options in the advanced crawl controls. `scripts/harness/xiaohongshu_note_type_check.mjs` guards the updated document link, new sort values, and `鐩存挱绗旇` mapping without external calls. `docs/harness/decisions.md`, `docs/harness/verification.md`, and `docs/harness/progress.md` were updated with the confirmed facts.

Verification passed: `node scripts/harness/xiaohongshu_note_type_check.mjs`; full `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`; read-only PostgreSQL queue status check showing no queued/running simple runs, simple queue items, or Feishu publish jobs; and `npm run local:restart` after the sandbox run hit the known `spawn EPERM` build limitation. The local production app is refreshed at `http://127.0.0.1:3001/`. No live TikHub/OpenAI-compatible/image-provider/Feishu production task was triggered.

Implementation completed on 2026-06-09 for the GPT-Image-2 backup Images API route. `src/lib/config.ts` now supports `OPENAI_IMAGE_BACKUP_BASE_URL` and `OPENAI_IMAGE_BACKUP_API_KEY`, and `/api/config` exposes only non-sensitive backup route status/base URL. `src/lib/image-generation.ts` now uses an in-process active Images API route for standard GPT-Image-2 `/images/generations` and multipart `/images/edits`: primary route failures switch to backup, and backup route failures switch back to primary. The failover predicate is limited to route-level failures such as auth/upstream/timeout/gateway/non-JSON failures; content-safety and invalid-image errors remain hard image errors.

Verification passed: `node scripts/harness/image_task_fallback_check.mjs`; `npx --no-install tsc --noEmit`; `npm run lint`; `npm run build`; and full `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`. No live TikHub/OpenAI-compatible image-provider/Feishu call was triggered. The local `http://127.0.0.1:3001/` production server was not restarted because read-only PostgreSQL status showed active run `simple-1780996444988` and its `simple_run_queue` item still `running` with a valid lock at `2026-06-09T09:14:35.101Z`; restarting would risk interrupting live work. Next entry point: after that run finishes, run `npm run local:restart` so the running local production server serves this backup-route implementation.

Implementation completed on 2026-06-09 for writing the content creator into generated-post Feishu records. `src/lib/feishu-cli.ts` now adds default field map key `contentCreationSource -> 鍐呭鍒涗綔鏉ユ簮` and writes the generated post owner display name to that Base field, falling back to the owner id when a historical post lacks a display name. `src/lib/feishu-publish-queue.ts` accepts `ownerDisplayName` in enqueue options, and both manual publish (`src/app/api/publish/feishu/route.ts`) and simple-run publish (`src/lib/simple-runs.ts`) pass the signed-in/run owner display name into the queued Feishu write.

Verification passed: `node scripts/harness/feishu_publish_queue_check.mjs`; `node scripts/harness/workspace_accounts_check.mjs`; `npx --no-install tsc --noEmit`; `npm run lint`; `npm run build`; full `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`; read-only PostgreSQL queue status check showing no queued/running simple or Feishu jobs; and `npm run local:restart`. The local app is refreshed at `http://127.0.0.1:3001/`. No live TikHub/OpenAI-compatible/image-provider/Feishu write was triggered by verification.

Implementation completed on 2026-06-09 for the latest simple link-mode generation failure. `src/lib/content-pool.ts` now retries `markSourceRewritten(...)` up to three times for PostgreSQL `40P01` deadlock and `40001` serialization conflicts. `src/lib/simple-runs.ts` now persists the generated post and records it in the simple-run snapshot before attempting source-item status sync; simple-run source-status sync failures are logged as non-fatal warnings and no longer hide an already-persisted draft or block Feishu publish enqueue. New regression guard: `scripts/harness/simple_run_persistence_check.mjs`, wired into `scripts/harness/check.ps1`.

Verification passed: `node scripts/harness/simple_run_persistence_check.mjs`; `node scripts/harness/content_projects_upsert_check.mjs`; `node scripts/harness/simple_queue_check.mjs`; `npx --no-install tsc --noEmit`; `npm run lint`; `npm run build`; full `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`; read-only PostgreSQL queue status check showing no queued/running simple or Feishu jobs; and `npm run local:restart`. The local app is refreshed at `http://127.0.0.1:3001/`. No live TikHub/OpenAI-compatible/image-provider/Feishu task was triggered by verification.

Read-only diagnosis completed on 2026-06-09 for the latest failed simple link-mode generation task. The failed run is `simple-1780987794365`, submitted at `2026-06-09T06:49:54.365Z` with keyword `灏忛箯GX` and source link `https://www.douyin.com/note/7642152026067847158`. Crawl/source-link import succeeded, source safety kept 1 item, Feishu source-import sync created the source record, AI source tagging succeeded with 2 content tags and 5 visual tags, and text generation succeeded. Local media was available: three cached images, local `video-1.mp4`, and selected video frames under `public/media/crawl/douyin/7642152026067847158/frames/`.

Root cause: the draft itself was persisted as `post-douyin-7642152026067847158-1780987905866` with title `灏忛箯GX杩欎釜浠锋牸杩樻湁璋佸晩` and five image URLs, but the simple-run production path then failed in the source-status update step with PostgreSQL error `妫€娴嬪埌姝婚攣`. The failure happened after `savePost(...)`/`saveGeneratedPost(...)` and before `addPostResult(...)`, so the run snapshot ended with `posts=0`, publish skipped, and errors `douyin-7642152026067847158: 妫€娴嬪埌姝婚攣` and `娌℃湁鎴愬姛鐢熸垚鍙彂甯冪殑鍥炬枃`. The corresponding content-pool item still has `poolStatus="new"` and `usedCount=0`, confirming `markSourceRewritten(...)` did not commit. Concurrent run `simple-1780987613000` overlapped the same window and generated 28 posts; image-provider logs also showed many `524`/timeout retries and source-image fallbacks, including one fallback for a 764215 keyframe, but those were not the fatal cause.

Inspection used read-only PostgreSQL queries through the app environment, local outbox/media inspection, `ffprobe` on the cached mp4, and code inspection. No TikHub/OpenAI-compatible/image-provider/Feishu calls were triggered, and no runtime data or media files were mutated. Next implementation entry point, if the user asks for a fix: make the simple-run persistence boundary resilient to PostgreSQL `40P01`/deadlock around `markSourceRewritten(...)` and/or narrow source-item status mutation away from full content-project JSON rewrites; then ensure a persisted draft cannot be hidden from the run summary when only source-status sync fails.

Live end-to-end source-link validation completed on 2026-06-09 for `https://www.douyin.com/note/7648946856000976549`. Initial run `simple-1780986477422` resolved the link, passed safety filtering, tagged content/visuals, and created the Feishu source record, but production skipped because the source was video-like and the video download aborted before any bytes, leaving no extracted frames. Direct probes isolated the cause to request headers: the `zjcdn.com/.../video/tos/...mime_type=video_mp4` URL returned `200 video/mp4` and content length 11,647,678 bytes, but aborted at 0 bytes with the old CDN-host Referer; it downloaded successfully with Douyin Referer/Origin and identity transfer encoding.

`src/lib/media-request.ts` now sends `Referer: https://www.douyin.com/` and `Origin: https://www.douyin.com` for Douyin media hosts and likely ByteDance video CDN hosts such as `zjcdn.com`, and sends `Accept-Encoding: identity` for likely video requests. `scripts/harness/media_request_headers_check.mjs` covers that failed CDN shape and is wired into `scripts/harness/check.ps1`.

Retest run `simple-1780986983004` passed after `npm run local:restart`: the same Douyin note cached three local images, local `video-1.mp4`, and five selected scene-change frames; source safety and tagging passed; one generated post was produced and published. Generated post `post-douyin-7648946856000976549-1780987043435` has title `灏忛箯GX闂ㄥ簵璇曢┚杩樻尯鎰夊揩`, five `video_frame`/`keep` final images, Feishu record id `recvm1G9e8AVRV`, and `attachmentStatus="uploaded"` with five attachments. Direct Feishu source-table search confirmed source record `recvm1DPSlAp4F` now has three image attachments and one `video-1.mp4` attachment. Feishu publish job `feishu-publish-1780987045967-ab75b834` completed and the configured notification was sent.

Verification passed for the focused change and final baseline: `node scripts/harness/media_request_headers_check.mjs`; `node scripts/harness/media_url_filter_check.mjs`; `node scripts/harness/media_cache_image_format_check.mjs`; `npx --no-install tsc --noEmit`; `npm run lint`; `npm run build`; read-only PostgreSQL queue checks; `npm run local:restart`; and full `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`. The live validation intentionally called TikHub, OpenAI-compatible text/vision generation, media/video extraction, Feishu source-import write/upload, Feishu generated-post write/upload, and Feishu notification; the final Harness baseline did not call external production services.

Implementation completed on 2026-06-09 for the latest simple link-mode source-import attachment miss. `src/lib/source-import-feishu.ts` now parses `record_id_list` from `lark-cli base +record-batch-create` output before falling back to object-shaped record ids, which unblocks the image/video attachment upload step after source-record creation. Existing duplicate source records are now searched with the source/image/video fields selected; when an exact duplicate exists but image or video attachment cells are empty, the sync uploads the missing local selected frames/images and/or local video to that existing record instead of skipping forever. `logSourceImportSync(...)` now includes `firstFailedSourceItemId` and compact `firstFailedSourceError` diagnostics.

`scripts/harness/source_import_feishu_check.mjs` now guards the new behavior: table-shaped `record_id_list` parsing, duplicate attachment repair, table-shaped attachment counts, first-failed source diagnostics, and the distinction between record-create fields and duplicate-search `select_fields`. Verification passed: `node scripts/harness/source_import_feishu_check.mjs`; `npx --no-install tsc --noEmit`; `npm run lint`; `npm run build`; and full `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`. No live TikHub/OpenAI-compatible/image-provider call, source-link import endpoint, Feishu record creation, Feishu attachment upload, or runtime media mutation was triggered. `npm run local:watch-simple -- -Once` returned `401` in private-read mode, so `http://127.0.0.1:3001/` was not restarted because queued/running simple work could not be safely ruled out from an unauthenticated check.

Read-only diagnosis completed on 2026-06-09 for the latest simple link-mode content-source attachment miss. Inspected run `simple-1780983242438` was submitted after the local app restart, completed generation, and published the generated post successfully. Its source item `douyin-7646791508233882920` has a local mp4 and five local selected video frames; the generated post used five `video_frame` tasks and generated-post Feishu publishing uploaded five attachments, so source parsing, media caching, frame extraction, production, and generated-post publish were not the failure point.

The separate Feishu source-import sync for that run logged `created=0`, `skippedDuplicate=0`, and `failed=1`. The outbox payload `data/feishu-outbox/source-import-record-1780983254651-4306c63a8120.json` proves the source-import path reached record-create payload generation with correct UTF-8 fields and a short canonical Douyin source link. Read-only Feishu CLI checks confirmed the target table fields and attachment field ids exist, short-keyword `record-search` works but finds no matching source record, and `record-batch-create --dry-run` can read the generated payload. No live source-table write or attachment upload was run.

Code inspection found the likely implementation break before attachment upload: `src/lib/source-import-feishu.ts` uploads image/video files only after `createSourceRecord(...)` returns a record id, but its `findRecordIds(...)` helper does not parse the `record_id_list` response shape handled by the main generated-post Feishu wrapper. Also, `logSourceImportSync(...)` records only aggregate counts and omits per-item errors, so the exact live source-record create error for this run is not recoverable from the execution log. Exact live create failure text remains 寰呯‘璁?until a user-approved source-table write test is run or the logging is improved and the flow is retried.

Verification after diagnosis passed: full `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`. No TikHub/OpenAI-compatible/image-provider call, Feishu record creation, Feishu attachment upload, or runtime media mutation was triggered.

Implementation completed on 2026-06-09 for the Feishu source-import table missing-write bug in recent simple link-mode runs. `src/lib/source-import-feishu.ts` now separates the source URL written to the Feishu source table from the keyword used for duplicate search. It writes canonical source URLs where possible, including short Douyin `https://www.douyin.com/note/{sourceId}` links, derives `record-search` keywords from `sourceId` or extracted content ids with a hard 50-character cap, and forbids the previous full-URL `keyword` path. Duplicate detection still exact-matches the source-link field after search and checks both canonical and historical raw parsed URLs.

`scripts/harness/source_import_feishu_check.mjs` now guards this behavior: source import must use a `SourceImportLink`, encode the 50-character search-keyword cap, canonicalize Douyin links, use `sourceLink.searchKeyword`, reject `keyword: sourceUrl`, and exact-match canonical/raw duplicate values after search. Verification passed: `node scripts/harness/source_import_feishu_check.mjs`; `npx --no-install tsc --noEmit`; `npm run lint`; `npm run build`; and full `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`. No live TikHub/OpenAI-compatible/image-provider call, Feishu record creation, attachment upload, runtime media mutation, or source-link import was triggered by verification.

Read-only diagnosis completed on 2026-06-09 for recent simple link-mode runs where generated posts published to Feishu but parsed source items did not appear in the Feishu source-import table. Runs `simple-1780978459712`, `simple-1780978706735`, and `simple-1780979073370` each resolved/imported one Douyin source item and published one generated post successfully, but logged a separate `Feishu source import sync` error with `created=0`, `skippedDuplicate=0`, and `failed=1`. The content-pool items exist and have safety assessments, so the source-link parsing and safety gate succeeded.

Root cause: `src/lib/source-import-feishu.ts` duplicate detection calls `lark-cli base +record-search` with the full parsed `sourceUrl` as `keyword`. TikHub normalized the recent Douyin `/note/...` inputs into long `iesdouyin.com/share/video/...` URLs with query strings; one inspected source URL was 473 characters while the submitted short link was 47 characters. A read-only Feishu CLI probe failed before record creation with API code `800010701`, message `String must contain at most 50 character(s).`, path `keyword`. Because `syncSourceItemsToFeishu(...)` catches per-item errors and logs the failed source-sync result instead of throwing, simple-run tagging, production, and queued generated-post Feishu publishing still complete. No code was changed by this diagnosis, and no TikHub/OpenAI-compatible/image-provider call, Feishu record creation, attachment upload, or runtime mutation was triggered.

Implementation completed on 2026-06-09 for user-imported source-link Feishu source sync. `src/lib/source-import-feishu.ts` owns the new sync path. Advanced `/api/crawl/links` imports and simple-run link mode now call it only after TikHub resolution and `filterUnsafeSourceItems(...)`, using `safetyResult.items`; unsafe filtered items, unsupported links, failed links, and keyword-mode simple crawls are not synced. The sync exact-matches existing Feishu records by `婧愰摼鎺, skips duplicates, creates records with `婧愰摼鎺, `鏍囬`, `姝ｆ枃`, and single-select `骞冲彴`, then uploads local downloaded images or selected/highlight frames to `鍥剧墖` and downloaded local video to `瑙嗛` via `lark-cli base +record-upload-attachment` using confirmed field ids.

Configuration defaults are in `src/lib/config.ts`: `FEISHU_SOURCE_IMPORT_ENABLED=true`, default Base token `JbpPbSIMqaD75wsZ9fAcBy9mnEe`, and default table id `tbllsn3LBZ6mWTyL`. `.env.example` and README document override knobs, including `imageFieldId`/`videoFieldId` in `FEISHU_SOURCE_IMPORT_FIELD_MAP` when moving to a different Base. The new static check is `scripts/harness/source_import_feishu_check.mjs` and it is wired into `scripts/harness/check.ps1`. Verification passed: new check, link import check, simple link run check, source safety check, `npx --no-install tsc --noEmit`, `npm run lint` with existing warnings only, `npm run build` with known Turbopack warnings only, full Harness baseline, read-only queue status check showing no queued/running simple runs before restart, and `npm run local:restart`. The refreshed app is running at `http://127.0.0.1:3001/`. No live TikHub/OpenAI-compatible/image-provider/Feishu write was triggered by verification.

Read-only analysis completed on 2026-06-09 for user-imported source-link production capacity. Current link-mode one-click production and advanced `/api/crawl/links` import both parse at most 200 source links per batch via `src/lib/source-link-import.ts`; simple link mode also normalizes to 200 unique non-empty links before enqueueing. A 100-link submission can enter one batch/run; a 1000-link submission is not automatically split and only the first 200 parsed links are processed. Simple link mode enqueues one durable `simple_run_queue` run, then resolves links through TikHub with bounded crawl concurrency before source safety, tagging, content-pool ingest, production, image generation, and queued Feishu publish. Verification passed: `node scripts/harness/link_import_check.mjs`, `node scripts/harness/simple_link_run_check.mjs`, and full `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1` with existing lint/Turbopack warnings only. No live TikHub/OpenAI-compatible/image-provider/Feishu task was triggered.

Implementation completed on 2026-06-09 for the video-link production fallback bug reported from Jacob's latest link-mode run. `src/lib/simple-runs.ts` now identifies video-like sources by `mediaType="video"`/`"mixed"`, video URL/local video presence, or `mediaCache.videoPresent`. In automatic simple production, video-like sources require extracted `videoFrames`; if the platform blocks video download and no frames exist, the source is skipped with the explicit message `Skipped video source without extracted video frames.` plus scalar diagnostics instead of using cover/origin/dynamic-cover images. Image-only imported links continue to use downloaded/source images.

`src/lib/creation-controls.ts` now applies the same default production-task rule: video-like sources return only video-frame tasks, and no-frame video-like sources return no default source/cover image tasks. `scripts/harness/simple_crawl_media_policy_check.mjs` now guards the simple eligibility rule and default task-builder rule. Verification passed: the new check failed before implementation and passed after the fix; `node scripts/harness/simple_crawl_media_policy_check.mjs`; `npx --no-install tsc --noEmit`; `npm run lint` with existing warnings only; `npm run build`; full `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`; read-only PostgreSQL queued/running status query; and elevated `npm run local:restart` after sandbox `spawn EPERM`. The local app is refreshed at `http://127.0.0.1:3001/`. No live TikHub/OpenAI-compatible image/text generation, image-provider generation, Feishu write, runtime media mutation, or new simple run was triggered.

Read-only diagnosis completed on 2026-06-09 for Jacob's latest link-mode Douyin video/mixed production that published only two visual outputs. Inspected run: `simple-1780970205710`, link `https://www.douyin.com/note/7646086708865596590`, keyword `灏忛箯GX`, target `1`, owner display `jacob`, created `2026-06-09T01:56:45.710Z`. It completed and published successfully. Generated post `post-douyin-7646086708865596590-1780970262792` stored `imageUrls.length=2` and `imageTasks.length=3`; the three tasks were `source_image` wash tasks for `/media/crawl/douyin/7646086708865596590/image-1.jpg`, `image-2.jpg`, and `image-3.jpg`, not `video_frame` tasks.

Root cause: the source item was `mediaType="mixed"` and had a video URL, but local video caching failed with `video-1: HTTP 403`; therefore no local video was saved, no `frames/` directory exists under `public/media/crawl/douyin/7646086708865596590`, and `videoFrames.length=0`. With no extracted frames, production fell back to the three cached Douyin cover/origin/dynamic-cover images as source references. During image generation, `鍘熷浘 3` failed before provider submission with `reference image resize failed` from ffmpeg/WebP decoding, while two GPT-Image-2 tasks succeeded. The final two images were already persisted in the generated post before Feishu publish, so Feishu did not remove images.

Quality explanation: the current frame-selection policy can rank only frames that were successfully extracted. It does not download protected Douyin video URLs through an alternate path and does not perform aesthetic/model-based best-frame search over the original video. When video download is blocked, the app uses available provider image/cover references, which may be less attractive than the best moments in the video. No code or runtime data was changed by this diagnosis, and no new TikHub/OpenAI-compatible image/text generation or Feishu production task was triggered.

Implementation completed on 2026-06-09 for the missing visible account-management menu report. Root cause: `AccountMenuV2` was rendered from the signed-in header, but the signed-in `.design-header` set `overflow: hidden`, clipping the absolute-positioned account popover below the header. `src/app/globals.css` now sets `.studio-topbar > .design-header { overflow: visible; }` and raises `.account-menu`/`.account-popover` z-index so clicking the top-right account chip such as `jacob ADMIN` exposes the account list and admin account form.

Verification passed: `npm run lint`, `npx --no-install tsc --noEmit`, `npm run build`, full `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`, read-only PostgreSQL status query confirming no `queued`/`running` simple runs or simple queue items before restart, and `npm run local:restart`. The refreshed app is running at `http://127.0.0.1:3001/`. `npm run local:watch-simple -- -Once` returned `401` under the current private-read account mode, so do not rely on that unauthenticated watcher before restarts unless it is updated to authenticate. No live TikHub/OpenAI-compatible image/text generation or Feishu production task was triggered.

Implementation completed on 2026-06-08 for whitelist account administration plus member content isolation. `src/lib/workspace-accounts.ts` defaults to `WORKSPACE_AUTH_MODE=whitelist`; `WORKSPACE_ALLOWED_USERS` controls allowed usernames, `WORKSPACE_ADMIN_USERS` controls first-admin/forced-admin usernames, and `WORKSPACE_ACCESS_PASSWORD` is only the first-admin setup key. Daily whitelist login now requires an active per-user account password hash in `workspace_accounts`. Admins can create, update, reset, disable, promote, and list accounts from the account menu and `GET|POST|PATCH /api/accounts`.

Member isolation is now app-layer owner-scoped. Normal members see only their own content projects/source items, generated posts, batch jobs, material folders/assets, execution logs, crawl jobs/runtime posts, simple runs, media-backfill targets, and Feishu publish jobs. Admins see all records. New writes stamp `ownerUserId`/`ownerDisplayName`; existing unowned records are admin-only under member filters. `requireWorkspaceAccount(...)` enters the execution-log owner context so route-level logs are also owned. Direct `/media/...` and `/generated/...` URL authorization is still a remaining V1.1 hardening item.

Verification passed: `node scripts/harness/workspace_accounts_check.mjs`, `node scripts/harness/link_import_check.mjs`, `node scripts/harness/generated_posts_upsert_check.mjs`, `npx --no-install tsc --noEmit`, `npm run lint`, `npm run build`, full `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`, read-only `npm run local:watch-simple -- -Once` before restart, `npm run local:restart`, and read-only `GET http://127.0.0.1:3001/api/accounts/session` confirming whitelist mode with an existing admin account, whitelist configured, setup key configured, and no current browser session. The refreshed app is running at `http://127.0.0.1:3001/`. No new TikHub/OpenAI-compatible image/text generation or Feishu production task was triggered by this verification.

Implementation completed on 2026-06-08 for replacing RunningHub image-to-image requests with GPT-Image-2 standard Images API requests. `src/lib/image-generation.ts` no longer contains or dispatches RunningHub image calls. Text-to-image uses JSON `POST /images/generations`; selected reference-image/image-to-image tasks use multipart `POST /images/edits` with binary `image` upload; multi-image requests loop as repeated `n: 1` calls. Local and remote reference images are normalized through `ffmpeg` before upload and temporary normalized files are cleaned after request completion/failure.

Updated files: `src/lib/image-generation.ts`, `src/lib/config.ts`, `src/lib/types.ts`, `.env.example`, `README.md`, `scripts/harness/image_task_fallback_check.mjs`, `scripts/harness/image_prompt_guard_check.mjs`, `scripts/harness/concurrency_check.mjs`, and Harness docs. `src/lib/config.ts` now supports `OPENAI_IMAGE_API_KEY` with `OPENAI_API_KEY` fallback, normalizes stale/unknown image endpoint values such as old `runninghub` to `images`, defaults `OPENAI_IMAGE_MODEL` to `gpt-image-2`, and `/api/config` exposes `openaiImageConfigured` without RunningHub paths. Local `.env.local` was updated without exposing secret values so the running local app uses `OPENAI_IMAGE_ENDPOINT=images` and `OPENAI_IMAGE_MODEL=gpt-image-2`. Verification passed: `node scripts/harness/image_task_fallback_check.mjs`, `node scripts/harness/image_prompt_guard_check.mjs`, `npm run lint`, `npx --no-install tsc --noEmit`, full `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`, read-only `npm run local:watch-simple -- -Once`, `npm run local:restart`, and read-only `GET http://127.0.0.1:3001/api/config` confirming `imageProvider=images`, `imageModel=gpt-image-2`, and `openaiImageConfigured=true`. No live TikHub/OpenAI-compatible GPT-Image-2/Feishu production task was triggered.

Live image-model smoke passed on 2026-06-08 after the replacement: a minimal configured `gpt-image-2` `POST /images/generations` request returned a PNG image URL, and the generated file was downloaded and validated locally at `/generated/image-model-smoke-1780917627229-20b2dab9-3d02-45b2-a9e6-d2d3a699107a.png` (1,066,891 bytes). This smoke called only the configured OpenAI-compatible image provider and did not trigger TikHub, Feishu, `/api/simple/runs`, or app account mutations.

Follow-up implementation completed on 2026-06-08 for V1 shared workspace account write-surface hardening. All remaining high-risk mutating workspace API routes now call `requireWorkspaceAccount(request)` before body parsing, local writes, queue creation, or external TikHub/OpenAI-compatible/RunningHub/Feishu paths. The protected surface now includes content item CRUD and batch actions, keyword crawl, source-link import, post generation, image generation, material library edits, material scan, batch production, generated-post CRUD and batch actions, post regeneration, review save/edit, and activity-log clearing, in addition to the previously protected simple-run, workspace-settings, and manual Feishu publish write paths. Read-only local diagnostics remain compatible.

Updated files: `src/lib/workspace-accounts.ts`, affected `src/app/api/**/route.ts` handlers, `scripts/harness/workspace_accounts_check.mjs`, `scripts/harness/http_smoke.js`, and Harness docs. Verification passed: `node scripts/harness/workspace_accounts_check.mjs`, `npm run lint`, `npx --no-install tsc --noEmit`, `npm run build`, full `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`, read-only `npm run local:watch-simple -- -Once`, and `npm run local:restart`. The refreshed local app at `http://127.0.0.1:3001/` now returns `401` for unauthenticated `POST /api/production/posts` during smoke. No live TikHub/OpenAI-compatible/RunningHub/Feishu production task was triggered, and verification did not create or expose account credentials.

Historical note from earlier on 2026-06-08, superseded by the current GPT-Image-2 replacement above: a half-merged `src/lib/image-generation.ts` state had temporarily restored the `runninghub` provider branch for both no-task generation and selected source-image tasks. The current implementation removes that RunningHub image-generation dispatch.

Historical note from earlier on 2026-06-08, superseded by the current GPT-Image-2 replacement above: image-generation API request settings were made separately configurable, including OpenAI-compatible image timeout settings and RunningHub timeout/path settings. The current image provider path no longer exposes or dispatches RunningHub image settings.

Historical verification for that superseded settings work passed with `node scripts/harness/image_task_fallback_check.mjs`, `npm run lint`, `npx --no-install tsc --noEmit`, full `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`, read-only `npm run local:watch-simple -- -Once`, `npm run local:restart`, and read-only `GET http://127.0.0.1:3001/api/config`. The RunningHub path/timeout fields observed in that older check are not current after the GPT-Image-2 replacement.

Implementation completed on 2026-06-08 for V1 shared workspace accounts. The new account system is implemented in `src/lib/workspace-accounts.ts`, `src/app/api/accounts/route.ts`, `src/app/api/accounts/session/route.ts`, `src/lib/database.ts`, `src/lib/types.ts`, `src/app/page.tsx`, and `src/app/globals.css`. Runtime schema now includes `workspace_accounts` and `workspace_sessions`; sessions use the HttpOnly `fluxpost_session` cookie; passwords are stored as Node `scrypt` hashes. The first local user bootstraps an `admin` account from the account access panel, and admins can add `operator` or `admin` accounts from the header account menu.

The workbench remains a shared data surface rather than per-user partitioned storage. Signed-in write paths now carry account identity: `POST /api/simple/runs` persists `ownerUserId`/`ownerDisplayName`, simple-run Feishu publishing uses the persisted run owner, and manual `POST /api/publish/feishu` uses the current account id. Read-only diagnostics remain compatible: `GET /api/simple/runs`, `GET /api/publish/feishu`, and `npm run local:watch-simple -- -Once` work without a browser session. Earlier automated account verification did not create an admin account; current runtime state may contain a user-created account, but Harness docs must not record or expose credentials.

Verification passed: `node scripts/harness/workspace_accounts_check.mjs`, `node scripts/harness/postgres_schema_check.mjs`, `npm run lint`, `npx --no-install tsc --noEmit`, `npm run build`, full `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`, `npm run local:watch-simple -- -Once`, `npm run local:restart`, local account API smoke on `http://127.0.0.1:3001`, and Playwright desktop/mobile account-access layout checks with no console/page errors or horizontal overflow. The local production app is refreshed at `http://127.0.0.1:3001/`. No live TikHub/OpenAI-compatible/RunningHub/Feishu production task was triggered.

Read-only diagnosis completed on 2026-06-08 for the user's report that the latest image-wash step did not take effect. Latest run inspected: `simple-1780912841661`, a simple link-mode Douyin run for `https://www.douyin.com/note/7648829665784109041`, keyword `灏忛箯P7`, target `1`, created `2026-06-08T10:00:41.661Z`. It completed and published successfully, but the generated post `post-douyin-7648829665784109041-1780912882948` saved only the original `/media/crawl/douyin/7648829665784109041/image-1.webp`. Evidence: the image task was `mode="wash"`, the reference image was normalized and uploaded to RunningHub, task `2063924340466257921` remained `RUNNING` through the 180-second local deadline, and the app recorded `Image task timed out; using source image`.

Broader recent audit: previous link-mode run `simple-1780912701274` saved 4 final images, with 3 original source URLs and 1 RunningHub URL. Across the inspected 17:47, 17:58, and 18:00 local completed runs, final generated posts contained 19 image URLs total: 3 RunningHub generated URLs and 16 original source URLs. Root cause is the current selected-image fallback policy in `src/lib/image-generation.ts`: timeout/gateway/5xx/429/provider-overload cases are treated as per-slot source-image fallback and still complete the post; some RunningHub tasks also failed non-recoverably with content-safety audit rejection. Feishu was not the cause because the generated posts already stored source URLs before publish. No code or runtime data was changed by the diagnosis, and no new live TikHub/OpenAI-compatible/RunningHub/Feishu production task was triggered.

Implementation completed on 2026-06-08 for the user's requested Feishu write bottleneck change. Feishu CLI writes now run through a durable `feishu_publish_queue` owned by `src/lib/feishu-publish-queue.ts` with schema/helpers in `src/lib/database.ts` and `db/migrations/001_initial_postgres.sql`. Manual `POST /api/publish/feishu` enqueues and returns `202` plus `jobId`; `GET /api/publish/feishu?jobId=...` supports frontend polling. Simple-run publish approves/saves posts, enqueues Feishu publishing with `publish.status="queued"` and `jobId`, then frees the simple-run worker; the Feishu queue worker later persists per-post Feishu state and updates the related simple-run publish stage. Queue claiming serializes Feishu writes per `ownerUserId` while allowing other owners to run independently.

Collection/generation defaults were raised while Feishu write pressure stayed conservative: crawl `12`, media `30`, production `30`, simple-run workers `4`; GPT remains `50`, image remains `100`, Feishu remains `50`, Feishu attachment remains `3`.

Verification passed: `node scripts/harness/feishu_publish_queue_check.mjs`, `node scripts/harness/feishu_publish_resume_check.mjs`, `node scripts/harness/concurrency_check.mjs`, `node scripts/harness/postgres_schema_check.mjs`, `node scripts/harness/simple_queue_check.mjs`, `npm run lint`, `npx --no-install tsc --noEmit`, `npm run build`, full `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`, read-only `npm run local:watch-simple -- -Once`, read-only `GET /api/simple/runs`, `npm run local:restart`, read-only `GET http://127.0.0.1:3001/api/publish/feishu` returning `jobs=[]`, and `node scripts/harness/http_smoke.js http://127.0.0.1:3001`. The local production app is refreshed at `http://127.0.0.1:3001/`. No live TikHub/OpenAI-compatible/RunningHub/Feishu production task was triggered by verification.

Implementation completed on 2026-06-08 for the user's report that 4 submitted batch/simple tasks only showed one progress item in the compact workspace bottom bar. Root cause: compact mode passed only `runForSummary` into `SimpleOverallProgressBar`, so it could never render the rest of `simpleRuns`. The fix is scoped to `src/app/page.tsx`, `src/app/globals.css`, and `scripts/harness/simple_queue_check.mjs`: compact mode now passes the full `runs` list, derives up to 8 queued/running live runs, shows an aggregate summary, renders one compact progress row per live run, preserves row selection into the shared simple-run state, and exposes per-row force terminate controls.

Verification passed: `node scripts/harness/simple_queue_check.mjs`, `npm run lint`, `npx --no-install tsc --noEmit`, `npm run build`, read-only `npm run local:watch-simple -- -Once`, read-only `GET http://127.0.0.1:3001/api/simple/runs` confirming no queued/running simple runs before restart, elevated `npm run local:restart`, mocked Playwright desktop/mobile checks confirming 4 compact progress rows with no horizontal overflow, and full `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`. The local production app at `http://127.0.0.1:3001/` has been refreshed. No live TikHub/OpenAI-compatible/RunningHub/Feishu production task was triggered by verification.

Diagnosis completed on 2026-06-08 for the user's report that the latest simple task failed during image/text generation. The latest run is `simple-1780898468621`, a simple link-mode run for `https://www.douyin.com/note/7644457387965633769` under keyword `灏忛箯GX`, target `1`, created `2026-06-08T06:01:08.621Z`. The watcher and `/api/simple/runs` show crawl/source-link import succeeded, AI tagging succeeded, text generation succeeded, `produce` failed, and publish was skipped. The decisive error is `All image tasks failed: 鍘熷浘 1: reference image resize failed: spawn EPERM`.

Root cause: the source image was valid and local (`public/media/crawl/douyin/7644457387965633769/image-1.webp`). Direct PowerShell `ffmpeg` conversion succeeded, sandboxed Node `child_process.execFile("ffmpeg", ...)` reproduced `spawn EPERM`, and the same Node diagnostic succeeded when run elevated/non-sandboxed. The app failed before submitting the image task to RunningHub, while normalizing the local reference image for model input. No code was changed. After confirming no simple runs were running or queued, elevated `npm run local:restart` refreshed `http://127.0.0.1:3001`; build and local HTTP smoke passed with only known Turbopack tracing warnings. No new live TikHub/OpenAI-compatible/RunningHub/Feishu production task was started by the diagnosis or restart.

Implementation completed on 2026-06-08 for the user's request to force-terminate a failed simple task and add a reusable force-terminate button. `src/lib/simple-runs.ts` exports `terminateSimpleRun(...)`; it marks the selected simple run as failed/interrupted, closes the matching `simple_run_queue` item, records `Simple run force terminated`, and guards later workflow saves from reviving a force-terminated run. `src/app/api/simple/runs/route.ts` supports `DELETE /api/simple/runs` with `runId`. `src/app/page.tsx` and `src/app/globals.css` add `寮哄埗缁堟` controls to the compact bottom progress bar and simple-mode run summary, shown for queued/running/failed runs that have not already been force-terminated.

The currently reported failed run `simple-1780887246915` (`灏忛箯GX`) was cleared through the new UI on the refreshed local app at `http://127.0.0.1:3001/`; `npm run local:watch-simple -- -Once` shows the recent activity entry `Simple run force terminated` with message `鐢ㄦ埛宸插己鍒剁粓姝㈣浠诲姟銆俙. Verification passed: `node scripts/harness/simple_queue_check.mjs`, `node scripts/harness/simple_link_run_check.mjs`, `npm run lint`, `npx --no-install tsc --noEmit`, `npm run build`, `npm run local:watch-simple -- -Once`, `npm run local:restart`, Playwright button check/click on `http://127.0.0.1:3001/`, and full `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`. No live TikHub/OpenAI-compatible/RunningHub/Feishu production task was triggered.

Implementation completed on 2026-06-08 for the user's follow-up request to add `鎵归噺瀵煎叆閾炬帴涓€閿敓浜у唴瀹筦 to both `绮剧畝鐗坄 and `绠€鍗曠増`. The simple workspace now has a `鍏抽敭璇嶉噰闆?/ 鎵归噺瀵煎叆閾炬帴` source switch shared by compact and standard variants. Link mode collects a required褰掑睘鍏抽敭璇?鍐呭姹犻」鐩? a controlled one-link-per-line textarea, an auto/forced platform selector, and a production limit that follows link count by default. The submit path posts `sourceMode="links"` to `POST /api/simple/runs`; it does not call `/api/crawl/links` from the browser.

Backend implementation is scoped to `src/lib/types.ts`, `src/app/api/simple/runs/route.ts`, `src/lib/simple-runs.ts`, and `src/lib/source-link-import.ts`. Link-mode simple runs enqueue through the existing durable `simple_run_queue`, resolve exact source links with the reusable `resolveSourceLinks(...)` helper, skip keyword platform search and top-up, then reuse the existing simple workflow stages for source safety filtering, AI tagging, content-pool ingest, post production, image handling, and Feishu publish. Per-link results are persisted on `SimpleRun.linkResults` for simple-mode display. Advanced `/api/crawl/links` remains available and unchanged as the content-pool-only import entry.

Verification passed: `node scripts/harness/simple_link_run_check.mjs`, `node scripts/harness/link_import_check.mjs`, `npm run lint`, `npx --no-install tsc --noEmit`, `npm run build`, read-only `npm run local:watch-simple -- -Once`, `npm run local:restart`, Playwright mocked compact/simple UI checks on `http://127.0.0.1:3001/` confirming two-link `sourceMode=links` payload submission without external-service calls, no console/page errors, and no horizontal overflow, and full `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`. The local production app at `http://127.0.0.1:3001/` has been refreshed.

Implementation completed on 2026-06-08 for the reported failed Douyin source-link import `https://www.douyin.com/note/7630774012096674665`. Local activity logs showed the old path sent the `/note/{id}` value to TikHub `/api/v1/douyin/web/fetch_one_video_v3`, which returned `404 Not Found`. The fix is scoped to `src/lib/tikhub.ts` and `scripts/harness/link_import_check.mjs`: Douyin `/note` source links now use `/api/v1/douyin/web/fetch_one_video_by_share_url`, `/video/{id}` links still use `/api/v1/douyin/web/fetch_one_video_v3`, single-video detail failures fall back to share-url, and Douyin source-link response normalization unwraps `aweme_detail`/`awemeDetail`.

Verification passed: `node scripts/harness/link_import_check.mjs`, `npm run lint`, `npx --no-install tsc --noEmit`, read-only `npm run local:watch-simple -- -Once`, `npm run local:restart`, live `POST http://127.0.0.1:3001/api/crawl/links` for the reported URL with `query="#灏忛箯GX"` returning `imported=1`, `sourceId=7630774012096674665`, `mediaType=image`, `imageCount=6`, `downloadedImageCount=6`, `videoFrames=0`, `taggedContent=1`, `taggedVisual=6`, local activity confirmation that TikHub used `/api/v1/douyin/web/fetch_one_video_by_share_url`, `node scripts/harness/http_smoke.js http://127.0.0.1:3001`, and full `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`. The live validation called TikHub and the configured OpenAI-compatible source safety/tagging path, but did not trigger RunningHub image generation or Feishu publish.

Implementation completed on 2026-06-08 for the user's request to add batch source-link import while preserving the existing architecture. The implementation is scoped to advanced content-pool collection: `src/app/page.tsx` adds a `鍏抽敭璇?/ 閾炬帴` mode switch in the crawl panel, controlled link textarea, optional platform override, import progress, and per-link result summary. `src/app/api/crawl/links/route.ts` is the thin API route. `src/lib/source-link-import.ts` owns batch parsing, per-link failure/duplicate handling, source safety filtering, AI tagging, and content-pool ingest. `src/lib/tikhub.ts` now owns supported source-link platform detection plus TikHub detail/share endpoint builders and single-link fetch normalization for Xiaohongshu, Douyin, Weibo, and WeChat Channels. `/api/crawl/jobs` keyword search and simple-mode one-click workflow were not changed.

Verification passed: `node scripts/harness/link_import_check.mjs`, `npm run lint`, `npx --no-install tsc --noEmit`, `npm run build`, full `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`, read-only `npm run local:watch-simple -- -Once`, `npm run local:restart`, and Playwright desktop/mobile checks on `http://127.0.0.1:3001/` confirming the advanced-mode `閾炬帴` import panel renders with no console/page errors and no horizontal overflow. No live TikHub/OpenAI-compatible/RunningHub/Feishu import or production task was triggered. The local production app at `http://127.0.0.1:3001/` has been refreshed.

Implementation completed on 2026-06-08 for the user's request to redesign the poor whole-page scrolling behavior and keep common top menus available. The fix is frontend-only: `src/app/page.tsx` now groups the header, workspace mode switcher, and advanced-mode command/module controls into `studio-topbar`, with the active workspace under `studio-body`; `src/app/globals.css` keeps the desktop shell locked to `100dvh`, keeps the topbar visible, and makes the relevant simple/advanced panels scroll internally. Compact mode's fixed bottom progress bar layout remains unchanged.

Verification passed: `npm run lint`, `npx --no-install tsc --noEmit`, `npm run build`, read-only `npm run local:watch-simple -- -Once`, `npm run local:restart`, Playwright desktop checks on `http://127.0.0.1:3001/` confirming simple mode panel scroll `0 -> 900` with `window.scrollY=0` and stable `.studio-topbar top=16`, advanced left panel scroll `0 -> 452` with `window.scrollY=0` and stable topbar, Playwright mobile 390px compact/advanced no-horizontal-overflow checks, and full `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`. No live TikHub/OpenAI-compatible/RunningHub/Feishu production task was triggered.

Current task on 2026-06-08: upload the latest local FluxPost Studio code snapshot to `https://github.com/Jacobshujun/fluxpost-studio.git`. The remote was checked with `git ls-remote` and returned no refs before upload. Full baseline verification passed with `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`; only known Turbopack tracing warnings and the expected Node SQLite experimental warning were emitted. Treat the repo as an initial source snapshot and keep `.env*`, runtime data, generated/cached media, `.tmp-*.json`, `test-artifacts/`, `.next/`, `node_modules/`, and `*.tsbuildinfo` out of Git.

Implementation completed on 2026-06-08 for the user's request to redesign the UI with the `frontend-design` skill. The redesign is visual/frontend-only: `src/app/globals.css` adds a 2026 command-desk redesign layer covering theme variables, app background, header, theme/config controls, workspace mode tabs, module tabs, panels, inputs, buttons, cards, simple-mode prompt strategy layout, fixed compact progress bar, review/package surfaces, and mobile overflow guards. `src/app/page.tsx` only adds `app-shell-${workspaceMode}` to the root `<main>` for mode-aware styling.

Verification passed: `npm run lint`, `npx --no-install tsc --noEmit`, `npm run build`, read-only `npm run local:watch-simple -- -Once`, `npm run local:restart`, Chrome headless/CDP screenshots for compact desktop/mobile and advanced desktop/mobile, CDP 390px compact-mobile check with `scrollWidth=390`, CDP advanced desktop/mobile checks with no horizontal overflow, and full `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`. The local production app at `http://127.0.0.1:3001/` is refreshed with the redesign. No live TikHub/OpenAI-compatible/RunningHub/Feishu production task was triggered. Browser screenshots are retained under `test-artifacts/ui-redesign/`; temporary Chrome CDP profile directories were removed before final baseline.

Implementation completed on 2026-06-05 for the user's report that recently crawled Weibo images could not display. Root cause: recent Sina image responses were cached as `.jpg` local files, but their actual bytes were HEIC (`ftypheic`), so browser previews that preferred local `/media/crawl/...` paths showed broken images. Some WebP responses were also cached under `.jpg`, which made extension-based `Content-Type` inference wrong.

Fix: `src/lib/image-format.ts` centralizes image byte sniffing; `src/lib/media-cache.ts` validates cached image bytes and converts HEIC cache files to JPEG in place with `ffmpeg`; `src/app/api/media/local/[...path]/route.ts` sniffs local image bytes before falling back to extension content types; `src/lib/source-tagging.ts` reuses the shared model-supported sniffing helper. Existing local Weibo cache was repaired in place: 13 HEIC files under `public/media/crawl/weibo` were converted to JPEG while keeping the original `/media/crawl/.../image-N.jpg` URLs, so runtime database records did not need mutation.

Follow-up after user retest: the remaining visible bad-image behavior was stale browser cache. The old broken HEIC response had the same `/media/crawl/.../image-N.jpg` URL and cacheable headers, so an already-open browser could reuse it after the file was repaired. `src/app/page.tsx` now appends `?v=20260605-image-format-v2` to local `/media/...` and `/generated/...` preview image URLs through `toDisplayImageSrc(...)`.

Implementation files: `src/lib/image-format.ts`, `src/lib/media-cache.ts`, `src/app/api/media/local/[...path]/route.ts`, `src/lib/source-tagging.ts`, `src/app/page.tsx`, `scripts/harness/media_cache_image_format_check.mjs`, `scripts/harness/source_tagging_image_check.mjs`, and `scripts/harness/check.ps1`. Verification passed: `node scripts/harness/media_cache_image_format_check.mjs`, `node scripts/harness/source_tagging_image_check.mjs`, `node scripts/harness/media_url_filter_check.mjs`, `node scripts/harness/weibo_search_mapping_check.mjs`, `npm run lint`, `npx --no-install tsc --noEmit`, read-only `npm run local:watch-simple -- -Once`, `npm run local:restart`, local HTTP media checks confirming `image/jpeg` and `image/webp`, Playwright image decode checks with non-zero dimensions, Playwright `灏忛箯GX` + Weibo content-pool check confirming 19 decoded images and cache-busted local media URLs, and full `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`. No new live TikHub/OpenAI-compatible/RunningHub/Feishu production work was triggered.

Implementation completed on 2026-06-05 for Weibo crawl debugging against TikHub docs page `https://docs.tikhub.io/410358109e0`. The current Weibo crawl path uses TikHub Weibo App search `/api/v1/weibo/app/fetch_search_all` with `query`, `page`, and numeric `search_type`; `includeType=pic` maps to `search_type=63`, and `includeType=video` maps to `search_type=64`. The old `/api/v1/weibo/web_v2/fetch_advanced_search` path and old `q`/`include_type`/`timescope` parameters should not be reintroduced.

Weibo App response normalization now uses a dedicated `mblog` extractor instead of generic array guessing, because the App payload can contain UI layout arrays that look record-like but are not posts. Weibo content images should come from `pics`/`pic_infos` and direct content image fields; broad raw-record URL fallback is disabled for Weibo to avoid avatars, icons, ad decorations, and layout media. Weibo CDN size variants are collapsed in `src/lib/media-url-filter.ts`, and local visual tagging now sniffs file bytes before inlining local images so HEIC bytes saved under `.jpg` are skipped rather than sent to the model as invalid JPEG.

Implementation files: `src/lib/tikhub.ts`, `src/lib/media-url-filter.ts`, `src/lib/source-tagging.ts`, `src/app/page.tsx`, `scripts/harness/weibo_search_mapping_check.mjs`, `scripts/harness/source_tagging_image_check.mjs`, and `scripts/harness/check.ps1`. Verification passed: `node scripts/harness/weibo_search_mapping_check.mjs`, `node scripts/harness/source_tagging_image_check.mjs`, `node scripts/harness/media_url_filter_check.mjs`, `npm run lint`, `npx --no-install tsc --noEmit`, full `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`, and `npm run local:restart`. Live minimal Weibo smoke on `http://127.0.0.1:3001/api/crawl/jobs` for keyword `Xiaopeng GX`, target `1`, hot + pic completed through `/api/v1/weibo/app/fetch_search_all` with `mediaType=image`, `imageCount=2`, `downloadedImageCount=2`, visual tagging `success`, `visualTags=1`, and one unsupported HEIC visual asset skipped. The live smoke called TikHub and the configured text/vision tagging model, but did not trigger RunningHub image generation or Feishu publish.

Read-only diagnosis completed on 2026-06-05 for the user's report that a recent simple-mode task appeared to use original images instead of washed/generated images. Latest run inspected: `simple-1780651475336` (`灏忛箯p7+ su7`, `douyin`, target `3`, created `2026-06-05T09:24:35.336Z`). It completed successfully with three published posts. All `14` selected image tasks were `mode="wash"`/`kind="source_image"`, so they were scheduled for image washing. Final output had `12` images: `8` exact source-task URL matches under `/media/crawl/...` and `4` RunningHub-generated/remote outputs. Retained logs for the run show `4` `RunningHub 鍥剧墖浠诲姟瀹屾垚`, `5` `Image task timed out; using source image`, and `2` hard `Image task failed` entries from RunningHub content safety audit rejection; earlier task start/submission/fallback logs were rotated out because execution logs retain only the latest `300` entries and this run produced `288` RunningHub wait logs.

Recent-run aggregation found mixed behavior rather than a global "never enters washing" bug. The latest `灏忛箯X9` run `simple-1780647321599` had `12` selected tasks with `6` `wash` and `6` `keep`; `keep` comes from the existing `鍐呴グ绌洪棿` visual-tag rule that intentionally keeps the original image. No business-code changes were made. Verification passed on 2026-06-05: `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`, with only known Turbopack tracing warnings and the expected Node SQLite experimental warning. Next entry point if the user wants a fix: improve observability/correlation for per-post image-task outcomes and decide whether timeout fallback should remain silent/automatic, be retried longer, or surface as a warning before Feishu publish. Default checks must not run a real simple task because that calls TikHub, model providers, RunningHub, and Feishu.

Implementation completed on 2026-06-05 for the compact simple workspace width adjustment requested by the user after the initial compact-mode delivery. The `绮剧畝鐗坄 task launch/control panel now spans the full workspace width instead of staying as a narrow left column with empty middle/right space. The compact panel uses a responsive grid: desktop lays keyword, target count, and platform controls across the top row and lets prompt strategy content expand across the full panel; mobile remains single-column. The fixed bottom overall progress bar remains visible.

Implementation files: `src/app/page.tsx` and `src/app/globals.css`. Verification passed on 2026-06-05: `npm run lint`, `npx --no-install tsc --noEmit`, `npm run local:restart`, Playwright desktop/mobile checks on `http://127.0.0.1:3001/` confirming full-width compact control panel, no `.simple-run-panel`, visible `.simple-overall-progress`, and no horizontal overflow, plus `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`. No new live TikHub/OpenAI-compatible/RunningHub/Feishu task was triggered by verification.

Implementation completed on 2026-06-05 for the compact simple workspace requested by the user. The app now has three workspace modes: `绮剧畝鐗坄, `绠€鍗曠増`, and `楂樼骇鐗坄, with `绮剧畝鐗坄 as the default. The compact mode reuses the existing simple-run backend and saved simple workspace settings, keeps only the left task launch/control panel in the main workspace, and adds a fixed bottom overall progress bar derived from `SimpleRun.stages`, platform results, generated posts, publish state, and run errors. The existing detailed simple-run status/results/history panel is still available when switching to `绠€鍗曠増`.

Implementation files: `src/app/page.tsx` and `src/app/globals.css`. Verification passed on 2026-06-05: `npm run lint`, `npx --no-install tsc --noEmit`, `npm run build`, elevated `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`, read-only `npm run local:watch-simple -- -Once`, elevated `npm run local:restart`, and elevated Playwright desktop/mobile checks on `http://127.0.0.1:3001/` confirming default compact mode, no rendered `.simple-run-panel`, visible `.simple-overall-progress`, no horizontal overflow, and no console/page errors. The local production server at `http://127.0.0.1:3001/` has been refreshed. No new live TikHub/OpenAI-compatible/RunningHub/Feishu task was triggered by verification.

Next entry point: manually use `绮剧畝鐗坄 from `http://127.0.0.1:3001/` and submit a small real task only when the user wants live workflow validation. The browser/UI verification was read-only and did not submit `/api/simple/runs`.

Implementation completed on 2026-06-05 for crawl-stage content safety filtering requested by the user to remove negative, profane, and competitor-bashing content during harvesting.

Fix: `src/lib/source-safety.ts` provides the source safety gate. It applies local hard-filter rules for obvious profanity/insult/competitor-bashing/strong-negative content, then calls the configured OpenAI-compatible text model (`appConfig.openaiTextModel`, currently `gpt-5.5` from `/api/config`) for non-hard-filter cases. Decisions are `allow`, `review`, or `filter`; kept items retain `safetyAssessment`, while filtered items are removed before AI tagging, content-pool ingest, simple-mode production, and Feishu publish. Advanced crawl applies `filterUnsafeSourceItems(...)` in `src/app/api/crawl/jobs/route.ts`; simple mode applies it in `src/lib/simple-runs.ts` after crawl/top-up and records `platformResults.filteredUnsafe`. The UI in `src/app/page.tsx` shows safety badges/cards for retained items and simple-mode safety-filter counts.

Verification passed on 2026-06-05: `node scripts/harness/source_safety_filter_check.mjs`, `npx --no-install tsc --noEmit`, `npm run lint`, `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`, read-only `npm run local:watch-simple -- -Once`, `npm run local:restart`, and a Playwright browser smoke on `http://127.0.0.1:3001/` with no console/page errors. Baseline and browser smoke did not trigger new live TikHub/OpenAI-compatible/RunningHub/Feishu production work.

Next entry point: manually validate with a small real crawl containing an obviously profane or competitor-bashing fixture/query when the user wants live confirmation. Expect execution-log action `Source safety filtered`, fewer retained items when unsafe content is found, `safetyAssessment` on retained items, and simple-mode platform rows showing `filteredUnsafe` counts.

Implementation completed on 2026-06-05 for the global video highlight-frame policy requested after the `灏忛箯X9` image-selection diagnosis. Video/mixed source items now expose at most 5 selected highlight frames instead of carrying the older 12-frame set into storage and downstream tasks.

Fix: `src/lib/video-frame-policy.ts` centralizes `maxVideoHighlightFrames=5`, frame ranking/deduplication, timestamp spacing, and stale frame URL cleanup for `mediaUrls`. The policy is applied in `src/lib/media-cache.ts`, `src/lib/media-backfill.ts`, `src/lib/content-pool.ts`, `src/lib/creation-controls.ts`, `src/lib/source-tagging.ts`, and `src/app/page.tsx`, so cache ingest, media backfill, content-pool refresh/update/create, default production image tasks, visual tagging, and frontend preview/editing share the same 5-frame cap. New regression coverage lives in `scripts/harness/video_frame_policy_check.mjs` and is wired into `scripts/harness/check.ps1`.

Verification passed on 2026-06-05: `node scripts/harness/video_frame_policy_check.mjs`, `node scripts/harness/source_tagging_image_check.mjs`, `node scripts/harness/simple_crawl_media_policy_check.mjs`, `node scripts/harness/media_url_filter_check.mjs`, `npx --no-install tsc --noEmit`, `npm run lint`, `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`, read-only `npm run local:watch-simple -- -Once`, and `npm run local:restart`. `http://127.0.0.1:3001` was refreshed and local HTTP smoke passed. No new live TikHub/OpenAI-compatible/RunningHub/Feishu production task was triggered by verification.

Next entry point: manually validate with a small video/mixed crawl when the user wants live confirmation. Check source `videoFrames.length <= 5`, content-pool preview, visual tag assets, and default production image tasks. Historical records with more than 5 frames are capped when read/refreshed/updated or when downstream paths build frame previews/tasks.

Implementation completed on 2026-06-05 for the latest `灏忛箯x9` Douyin one-click image-selection issue. Read-only diagnosis found recent runs `simple-1780630135757` and `simple-1780630487482` already carried saved `platformCrawlSettings.douyin.contentType="2"` (image), but the accepted Douyin source items were video-like cards normalized as `mediaType="mixed"` with video cover URLs and extracted `videoFrames`. The frontend selection/request payload was not the cause.

Fix: `src/lib/tikhub.ts` now treats Douyin `content_type=2` as image-only during result normalization. It keeps only records with true Douyin carousel/image fields detected by `extractDouyinCarouselImageUrls(...)`, skips video-cover-only candidates with a `Douyin image content-type mismatch skipped` activity-log entry, and strips direct video media from kept image results so image-only crawls do not produce `videoFrames`. Regression coverage was added to `scripts/harness/douyin_search_mapping_check.mjs`.

Verification passed on 2026-06-05: `node scripts/harness/douyin_search_mapping_check.mjs`, `node scripts/harness/douyin_carousel_image_check.mjs`, `npx --no-install tsc --noEmit`, `npm run lint`, `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`, read-only `npm run local:watch-simple -- -Once`, and approved elevated `npm run local:restart` after the expected sandbox `spawn EPERM`. `http://127.0.0.1:3001` was refreshed and local HTTP smoke passed. No new live TikHub/OpenAI-compatible/RunningHub/Feishu production task was triggered by verification.

Next entry point: if the user wants live validation, run a small Douyin `灏忛箯x9` image-only simple/advanced crawl and confirm the activity log either keeps true carousel image cards or reports skipped video-like candidates; do not use a real simple run as default baseline because it calls TikHub, model providers, RunningHub, and Feishu.

Implementation completed on 2026-06-05 for the latest large simple/batch-style run failure. Simple-mode crawl now performs one top-up pass after the initial even per-platform split when deduped candidates are below the target. Simple-mode automatic production skips no-media source items with no `downloadedImages`, no source `images`, and no `videoFrames`. Feishu publish now uses low attachment concurrency, returns `attachment_failed` for incomplete attachments after record creation/reuse, persists per-post `feishu.recordId` and attachment status, and redacts CLI token output. The local production app at `http://127.0.0.1:3001` was refreshed after implementation.

Verification passed after implementation on 2026-06-05: `node scripts/harness/feishu_publish_resume_check.mjs`, `node scripts/harness/simple_crawl_media_policy_check.mjs`, `node scripts/harness/concurrency_check.mjs`, `npx --no-install tsc --noEmit`, `npm run lint`, `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`, read-only `npm run local:watch-simple -- -Once`, and approved elevated `npm run local:restart` after the expected sandbox `spawn EPERM`. No new live TikHub/OpenAI-compatible/RunningHub/Feishu production task was triggered by verification.

Next entry point: manually validate with a moderate simple-mode batch when the user is ready. Confirm crawl top-up behavior, skipped no-media item counts, generated post count, and Feishu `attachment_failed` resume behavior if attachment upload throttling recurs. Do not blindly rerun the historical failed publish payload because existing Feishu Base records may already exist; retry through posts that carry persisted `feishu.recordId`.

Latest read-only diagnosis on 2026-06-05: the latest large simple/batch-style run is `simple-1780584174898` (`灏忛箯GX`, `xiaohongshu,douyin`, target `50`). It finished `partial`: crawl produced `44` candidates (`xiaohongshu=25/25`, `douyin=19/25`), tagging succeeded for all `44`, production generated `42` drafts, and publish failed during Feishu attachment upload. Two production failures were no-media Douyin items (`douyin-douyin-8` and `douyin-douyin-18`) that had no images, no downloaded images, and no video frames, then hit RunningHub non-JSON `504 Gateway Time-out` in the text-to-image path.

Feishu diagnosis for `simple-1780584174898`: outbox `data/feishu-outbox/posts-1780584814861.json` contains `42` posts and `114` image URLs. Read-only local checks confirmed `81` local references exist and `33` remote images were materialized under `public/generated/feishu-attachments`, so missing attachment files were not the cause. Failure happened during `lark-cli base +record-upload-attachment` after record payload creation; exact CLI stderr/stdout is not preserved in the compact simple-run error. Do not blindly rerun the full publish for this run, because Base records may already exist and duplicate records are possible. Recommended next implementation entry points: `src/lib/feishu-cli.ts` for attachment concurrency/idempotent resume/full sanitized CLI error capture; `src/lib/simple-runs.ts` for no-media source policy and cross-platform top-up; new Harness checks should avoid real Feishu/RunningHub calls.

Verification passed after diagnosis on 2026-06-05: `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`. No code changes were made and no live TikHub/OpenAI-compatible/RunningHub/Feishu task was triggered by verification.

Advanced-mode crawl strategy explicit save completed on 2026-06-04. Simple mode already uses saved `workspaceSettings.platformCrawlSettings`; the missing control was an operator-facing save action that does not start a crawl. `src/app/page.tsx` now has `saveCurrentPlatformCrawlSettings()` and a `淇濆瓨閲囬泦绛栫暐` button above `寮€濮嬮噰闆哷. The handler snapshots the current platform controls with `getWorkspaceSettingsWithCurrentPlatformCrawlSetting()`, persists through `persistWorkspaceSettings(...)`, updates local workspace settings, and reports `閲囬泦绛栫暐宸蹭繚瀛橈紝绠€鍗曠増浼氳嚜鍔ㄤ娇鐢ㄥ綋鍓嶅钩鍙拌缃甡. It does not call `/api/crawl/jobs`, and shared strategy persistence still excludes Douyin `cookie`.

Regression coverage: `scripts/harness/crawl_strategy_save_check.mjs` is wired into `scripts/harness/check.ps1`. Verification passed on 2026-06-04: the new check failed before implementation and passed after; `npm run lint`; `npx --no-install tsc --noEmit`; `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`; `npm run local:watch-simple -- -Once` confirmed the latest simple run was completed before restart; `npm run local:restart` refreshed `http://127.0.0.1:3001`. No new real TikHub/OpenAI-compatible/RunningHub/Feishu task was triggered by verification.

Latest simple-mode target-3/only-2-produced issue fixed on 2026-06-04. Read-only diagnosis found run `simple-1780581455008` for Douyin keyword `灏忛箯x9`, target `3`, crawled/tagged three items but produced/published only two posts. Source item `douyin-7583713246399286537` failed production because both selected image tasks received RunningHub non-JSON `504 Gateway Time-out` HTML, and the selected-task path treated this as `All image tasks failed`.

Fix: `src/lib/image-generation.ts` now has `isImageTaskSourceFallbackError(...)`, which treats timeouts, gateway/5xx/429, and temporary provider overload/rate-limit errors as recoverable per selected source-image task. Those tasks now log `Image task failed; using source image` and return the original `task.url` for that slot; non-recoverable image errors still fail. The older serial selected-task path has the same fallback behavior. Regression coverage: `scripts/harness/image_task_fallback_check.mjs` is wired into `scripts/harness/check.ps1`.

Verification passed on 2026-06-04: `node scripts/harness/image_task_fallback_check.mjs` failed before implementation and passed after it; `node scripts/harness/image_prompt_guard_check.mjs`; `npm run lint`; `npx --no-install tsc --noEmit`; `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`; read-only `/api/simple/runs` confirmed no active running/queued simple run before restart; `npm run local:restart` refreshed `http://127.0.0.1:3001`. No new real TikHub/OpenAI-compatible/RunningHub/Feishu task was triggered by verification. Historical run `simple-1780581455008` remains a partial runtime record and was not mutated.

Latest simple-mode RunningHub empty-prompt failure fixed on 2026-06-04. Live read-only diagnosis found recent Douyin simple runs for `灏忛箯GX` and `灏忛箯P7` included a no-media source item `douyin-douyin-8` with no images, no downloaded images, no video frames, and no visual assets. When the text model returned an empty `imagePrompt` for that no-task item, the no-selected-task image generation path submitted an empty `prompt` to RunningHub and received error `1007` (`field 'prompt' is required, can not be empty`).

Fix: `src/lib/simple-runs.ts` now resolves a fallback simple-mode image prompt from draft/source context before image generation and stores the actual prompt on the generated post; `src/lib/image-generation.ts` normalizes provider prompts and skips locally with an execution-log entry when there are no selected image tasks and no prompt, so empty prompts are not sent to RunningHub/OpenAI image providers; `src/lib/creation-controls.ts` tolerates missing runtime image-task `prompt` fields. Regression coverage: `scripts/harness/image_prompt_guard_check.mjs` is wired into `scripts/harness/check.ps1`.

Verification passed on 2026-06-04: `node scripts/harness/image_prompt_guard_check.mjs` failed before the implementation and passed after it; `npm run lint`; `npx --no-install tsc --noEmit`; `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`; read-only `/api/simple/runs` confirmed no active running/queued simple run before restart; `npm run local:restart` refreshed `http://127.0.0.1:3001`. No new real TikHub/OpenAI-compatible/RunningHub/Feishu task was triggered by verification. Historical partial simple-run records remain historical runtime state and were not mutated.

Simple-mode high-throughput V1/V2 completed on 2026-06-04. V1: simple-mode target count is now configurable with backend fallback `500` and hard ceiling `2000`, and the simple UI accepts up to `500`; Feishu Base record creation is chunked at `50` posts per request while attachment uploads remain per created record/chunk. V2: `POST /api/simple/runs` now saves a simple run, enqueues `simple_run_queue`, returns immediately, and starts in-process workers that claim queued work from PostgreSQL with `FOR UPDATE SKIP LOCKED` or from the SQLite fallback path.

Important runtime behavior: queued simple runs survive local server restart and will be picked up when `/api/simple/runs` is posted/read. Already-running stale runs are still marked interrupted instead of being blindly replayed, because Feishu record creation is not yet idempotently mapped per post and automatic replay after a publish-stage crash could duplicate Base records. Each simple run persists the platform crawl settings captured at submission time.

Implementation entry points: `src/lib/simple-runs.ts` owns queue worker startup, run snapshot settings, and simple workflow execution; `src/lib/database.ts` owns `simple_run_queue` schema/helpers and the PostgreSQL `SKIP LOCKED` claim; `src/lib/feishu-cli.ts` owns `feishuRecordBatchSize = 50` and chunked Base writes; `src/app/page.tsx` exposes the simple count input up to `500`; `scripts/harness/simple_queue_check.mjs` guards this behavior.

Verification passed on 2026-06-04: `node scripts/harness/simple_queue_check.mjs`, `node scripts/harness/simple_config_sync_check.mjs`, `npm run db:migrate:postgres -- --dry-run`, `npm run lint`, `npx --no-install tsc --noEmit`, `npm run build`, `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`, read-only `GET /api/simple/runs` confirming no active running/queued simple run before restart, and `npm run local:restart`. `http://127.0.0.1:3001` was refreshed. No real TikHub/OpenAI-compatible/RunningHub/Feishu production task was triggered by verification.

Latest simple-mode publish failure fixed on 2026-06-04. The failed run was `simple-1780572602107` for Douyin keyword `灏忛箯GX`, target `3`; crawl/tag/production completed and three drafts were generated, but publish failed with PostgreSQL duplicate-key text for `content_projects_pkey`. This was local content-pool persistence during source status updates, not a Feishu Base unique constraint.

Root cause: `writeContentProjectsToDb(...)` in `src/lib/database.ts` still used full-table replacement for `content_projects`. Concurrent simple-mode production/publish paths call `markSourceRewritten(...)` for multiple posts, and overlapping PostgreSQL delete/reinsert transactions could collide on the same content project primary key. Fix: `writeContentProjectsToDb(...)` now uses row-level upsert for both PostgreSQL and SQLite, preserving the original `created_at` and updating project metadata/JSON. New check `scripts/harness/content_projects_upsert_check.mjs` is wired into `scripts/harness/check.ps1`.

Verification passed on 2026-06-04: `node scripts/harness/content_projects_upsert_check.mjs`, `npm run lint`, `npx --no-install tsc --noEmit`, `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`, and `npm run local:restart`. `http://127.0.0.1:3001` was refreshed. No new real TikHub/OpenAI-compatible/RunningHub/Feishu task was triggered by verification. Historical run `simple-1780572602107` remains a partial failed runtime record and was not mutated.

Latest simple-mode failure fixed on 2026-06-04. The failed run was `simple-1780571959657` for Douyin keyword `灏忛箯GX`, target `5`; read-only `/api/simple/runs` and `/api/activity` showed TikHub `/api/v1/douyin/search/fetch_general_search_v1` returned `200` for the first request and `400` for the next pagination request, after which simple mode reported zero producible content.

Root cause: `fetchDouyinKeywordSearch(...)` in `src/lib/tikhub.ts` still tried to collect up to `targetCount * 6` candidates even though current product rules allow only dedupe/slice after provider results. A later pagination error threw out of the whole crawl and discarded first-page candidates. Fix: Douyin keyword pagination now stops once the requested deduped `targetCount` is collected; if a later page fails after candidates have been collected, those candidates are kept and a `Douyin keyword pagination stopped` execution-log entry is recorded. First-page failures still fail the crawl.

Regression coverage: `scripts/harness/douyin_search_mapping_check.mjs` now simulates both enough-first-page and partial-then-400 cases without calling TikHub, while still checking endpoint, sort/content-type mapping, cursor, and `search_id/backtrace` carry-through. Verification passed on 2026-06-04: `node scripts/harness/douyin_search_mapping_check.mjs`, `npm run lint`, `npx --no-install tsc --noEmit`, `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`, and `npm run local:restart`. `http://127.0.0.1:3001` was refreshed; no new real TikHub/OpenAI-compatible/RunningHub/Feishu task was triggered by verification.

Simple/advanced one-click production config sync was rechecked and fixed on 2026-06-04. Confirmed synchronized before this task: default text prompt, visual-tag image strategy prompts, image size/quality, and per-platform crawl settings. Confirmed bug fixed in this task: simple-mode backend production previously used `materialPaths: []`, so advanced scanned assets and material-library asset paths were not available to one-click generation.

Current code path: `src/app/page.tsx` sends `productionMaterialPaths` in the `POST /api/simple/runs` payload and shows the material count in the simple one-click strategy badges; `src/app/api/simple/runs/route.ts` accepts/forwards `materialPaths`; `src/lib/simple-runs.ts` normalizes and persists the paths in `SimpleRunInput`, logs only `materialCount`, and passes `normalizedInput.materialPaths` to `generatePost(...)`. `scripts/harness/simple_config_sync_check.mjs` is wired into the baseline and checks this chain without external calls.

Verification passed on 2026-06-04: `node scripts/harness/simple_config_sync_check.mjs`, `npm run lint`, `npx --no-install tsc --noEmit`, `npm run build`, `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`, `npm run local:watch-simple -- -Once`, and `npm run local:restart`. `http://127.0.0.1:3001` was refreshed. No real TikHub/OpenAI-compatible/RunningHub/Feishu task was triggered by verification. The latest partial simple run with `generated_posts_pkey` is historical old-code runtime state and was not mutated.

PostgreSQL migration bug sweep completed on 2026-06-04. Read-only checks on `http://127.0.0.1:3001` confirmed the app is running on PostgreSQL; the latest simple run still shows a historical partial publish failure from the old code path with `generated_posts_pkey`, and recent activity showed Douyin visual tagging failures because remote image URLs were passed directly to the OpenAI-compatible chat endpoint and rejected as invalid image data.

`src/lib/generated-posts.ts` and `src/lib/database.ts` now use row-level generated-post mutations: save/update/status changes go through upsert, and single/batch delete use generated-post delete helpers. This closes the remaining PostgreSQL full-table replacement collision window for generated-post runtime paths. `scripts/harness/generated_posts_upsert_check.mjs` is wired into the Harness baseline.

`src/lib/source-tagging.ts` now preflights remote visual assets before model tagging. Remote HTTP(S) images are fetched with shared media headers, validated as supported JPEG/PNG/GIF/WebP, size-limited, converted to inline data URLs, and unsupported assets are skipped per image with an execution-log entry instead of failing the whole batch. `scripts/harness/source_tagging_image_check.mjs` is wired into the Harness baseline and uses mocked local fetches only.

Verification passed on 2026-06-04: `node scripts/harness/source_tagging_image_check.mjs`, `node scripts/harness/generated_posts_upsert_check.mjs`, `npm run lint`, `npx --no-install tsc --noEmit`, `npm run build`, `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`, `npm run local:restart`, `node scripts/harness/http_smoke.js http://127.0.0.1:3001`, and read-only `/api/config` confirmed `databaseBackend=postgres`. No new real TikHub/OpenAI-compatible/RunningHub/Feishu task was triggered by verification. Existing historical simple-run records were not mutated.

Latest Douyin carousel fix on 2026-06-04: the live `灏忛箯X9` Douyin image/text crawl exposed the root cause of repeated images and missing later carousel images. TikHub general search V1 returns real carousel assets in `raw.images`, but the old extraction path read `video.cover/origin_cover` first and then broad recursive fallback URLs, so cover/jpeg/webp/heic/watermark variants of the same first few assets filled the image limit.

`src/lib/douyin-media.ts` now owns lightweight Douyin carousel image extraction. It chooses one best supported URL per carousel image, preferring non-watermarked JPEG/WebP `tplv-dy-aweme-images-v2` variants and avoiding HEIC, watermarked URLs, cover variants, and static UI assets. `src/lib/tikhub.ts` uses this helper before video-cover fallback, and `src/lib/media-url-filter.ts` canonicalizes Douyin variants by asset id.

Existing Douyin content-pool records can now be repaired from preserved raw data: `src/lib/content-pool.ts` uses `raw.images` to rebuild Douyin image lists during read refresh and drops old `downloadedImages` when the raw repair changes source-image order, because old local `image-1`, `image-2`, ... files were downloaded from the stale variant order.

Verification passed on 2026-06-04: `node scripts/harness/douyin_carousel_image_check.mjs`, `node scripts/harness/douyin_search_mapping_check.mjs`, `node scripts/harness/media_url_filter_check.mjs`, `node scripts/harness/xiaohongshu_note_type_check.mjs`, `npm run lint`, `npx --no-install tsc --noEmit`, `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`, `npm run local:watch-simple -- -Once`, and `npm run local:restart`. Post-restart read-only check on `http://127.0.0.1:3001/api/content-pool` confirmed current `灏忛箯X9` Douyin sample `sourceId=7647456408942589300` now exposes `images=32`, `downloadedImages=0`, `cacheStatus=remote_only`, `remoteImages=32`, and no HEIC/watermark/cover variants. No new real TikHub/OpenAI-compatible/RunningHub/Feishu task was triggered by verification.

Latest Douyin crawl update on 2026-06-04: per user instruction, Douyin keyword search was switched to TikHub `/api/v1/douyin/search/fetch_general_search_v1`. `src/lib/tikhub.ts` now builds general-search POST bodies through `buildDouyinKeywordSearchPayload(...)` with `keyword`, `cursor`, `sort_type`, `publish_time=0`, `filter_duration=0`, `content_type`, `search_id`, `backtrace`, and the optional per-request cookie.

Current Douyin mapping is confirmed before provider calls: sort `0/general/relevance -> sort_type=0`, `1/most_liked/likes_desc -> sort_type=1`, `2/latest/time_descending/published_desc -> sort_type=2`; content type `0/all -> content_type=0`, `1/video -> content_type=1`, `2/image/picture -> content_type=2`, and `3/article/text -> content_type=3`. `src/app/page.tsx` points the Douyin docs link at the new general-search V1 docs.

New Harness check `scripts/harness/douyin_search_mapping_check.mjs` is wired into `scripts/harness/check.ps1` and verifies the new endpoint, no old `fetch_video_search_v2` usage, the sort/content-type maps, `cursor` pagination, and `search_id/backtrace` carry-through without calling TikHub.

Verification passed on 2026-06-04: `node scripts/harness/douyin_search_mapping_check.mjs`, `npx --no-install tsc --noEmit`, `npm run lint`, `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`, `npm run local:watch-simple -- -Once`, and `npm run local:restart`. `http://127.0.0.1:3001` was refreshed and local HTTP smoke passed. No real TikHub/OpenAI-compatible/RunningHub/Feishu task was triggered by verification.

Latest Xiaohongshu duplicate-image fix on 2026-06-04: the latest local `灏忛箯P7` sample `sourceId=6a1b0255000000003700fbcd` had `images.length=10` but only five Xiaohongshu asset ids because App V2 returned each image as a blurry `!nd_prv...` URL plus a clearer `!nd_dft...` URL. `src/lib/media-url-filter.ts` now canonicalizes Xiaohongshu `xhscdn` / `rednotecdn` asset ids by stripping signed prefix differences and `!nd_prv` / `!nd_dft` transform suffixes, and `scoreImageUrl(...)` prefers `!nd_dft`.

Stale local cache handling was also fixed: `filterAlignedDownloadedImages(...)` drops `downloadedImages` when old local download count exceeds the normalized source-image count, because old local indexes can point to alternating blurry/clear variants. `src/lib/media-cache-status.ts` now counts only safely aligned local images when source images exist. New Harness check `scripts/harness/media_url_filter_check.mjs` is wired into `scripts/harness/check.ps1` and covers this regression.

Verification passed on 2026-06-04: `node scripts/harness/media_url_filter_check.mjs`, `npm run lint`, `npx --no-install tsc --noEmit`, `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`, and `npm run local:restart`. Local post-restart read-only check on `http://127.0.0.1:3001/api/content-pool` confirmed the `灏忛箯P7` sample now returns `imagesCount=5`, `downloadedCount=0`, `imagesAreDefault=true`, `cacheStatus=remote_only`, `localImages=0`, and `remoteImages=5`.

Latest read-only duplicate-image diagnosis on 2026-06-04: Xiaohongshu App V2 image records can produce visually duplicated images before media caching. The latest local sample `sourceId=6a1d3e9c000000003501cdd5` has `images.length=8` but only four Xiaohongshu asset ids; each asset appears once as a `!nd_prv...` preview URL and once as a `!nd_dft...` default URL. `downloadedImages.length=8` because `cacheCrawledMedia(...)` downloads every source image entry in order.

Root cause to fix next if requested: `extractXiaohongshuMedia(...)` currently calls the recursive generic `extractUrlList(...)` for each App V2 `imageList` item, collecting `urlPre`, `urlDefault`, and `infoList` scene URLs (`WB_PRV` / `WB_DFT`). Existing `normalizeContentImageUrls(...)` does asset-level dedupe, but `getImageAssetKey(...)` does not yet canonicalize `rednotecdn.com` signed prefixes or strip Xiaohongshu `!nd_prv` / `!nd_dft` transform suffixes. `mergeXiaohongshuDetail(...)` and `cacheCrawledMedia(...)` can further amplify visible duplication in `mediaUrls` by keeping search/detail remote URLs plus local downloads.

Verification for this read-only diagnosis passed on 2026-06-04: `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`. The command did not trigger TikHub, OpenAI-compatible services, RunningHub, or Feishu production calls; build emitted only the known Turbopack tracing warnings.

Latest Xiaohongshu crawl update on 2026-06-04: per user instruction, Xiaohongshu keyword search was switched from Web V3 search to TikHub App V2 `/api/v1/xiaohongshu/app_v2/search_notes`. `src/lib/tikhub.ts` now sends App V2 parameters `keyword`, `page`, `sort_type`, `note_type`, `time_filter=涓嶉檺`, `source=explore_feed`, and `ai_mode=0`; pagination carries returned `search_id` and `search_session_id` when available.

Current Xiaohongshu type mapping: internal image/text `noteType=2 -> note_type=鏅€氱瑪璁癭, video `noteType=1 -> note_type=瑙嗛绗旇`, and all/undefined `noteType -> note_type=涓嶉檺`. This supersedes the earlier Web V3 `note_type=1/2/0` mapping.

Verification passed on 2026-06-04: `node scripts/harness/xiaohongshu_note_type_check.mjs`, `node scripts/harness/keyword_relevance_check.mjs`, `node scripts/harness/concurrency_check.mjs`, `npm run lint`, `npx --no-install tsc --noEmit`, `npm run build`, `npm run local:restart`, and `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`.

Live validation on `http://127.0.0.1:3001`: an advanced Xiaohongshu crawl for keyword `灏忛箯G6`, sort `time_descending`, image/text `noteType=2`, target `1`, called `GET /api/v1/xiaohongshu/app_v2/search_notes`, logged `requestedNoteTypeParam=鏅€氱瑪璁癭, and returned one image item titled `琚繖鍙板皬楣廏6纭帶浜嗭紒` with real `sourceId=6a213dcf000000003503291b`, `contentText` length 160, six source images, six downloaded images, and zero video frames.

Latest update on 2026-06-04: the current product rule is no post-crawl hard filtering. The user chooses platform, keyword, sort, and type before crawling, so `src/lib/tikhub.ts` now relies on TikHub request parameters and then only dedupes, slices to `targetCount`, enriches/caches media, tags, and persists.

Removed/superseded crawl filters: local keyword relevance filtering, Xiaohongshu local image/video post-filtering, user-unselected all-type fallback retries, and cross-platform result dropping in `src/app/api/crawl/jobs/route.ts`. Xiaohongshu now maps UI note type to App V2 request parameter values: image/text `noteType=2 -> note_type=鏅€氱瑪璁癭, video `noteType=1 -> note_type=瑙嗛绗旇`, all `noteType=0/undefined -> note_type=涓嶉檺`.

Harness checks now enforce the no-filter policy and App V2 search mapping: `scripts/harness/keyword_relevance_check.mjs` fails if keyword relevance helper code returns, and `scripts/harness/xiaohongshu_note_type_check.mjs` fails if Xiaohongshu Web V3 search usage, local type filter helpers, dropped-count diagnostics, or selected-type all-note fallback return. Older strict-filter notes below are historical context only and must not guide new crawl changes.

Verification for this update passed on 2026-06-04: `node scripts/harness/keyword_relevance_check.mjs`, `node scripts/harness/xiaohongshu_note_type_check.mjs`, `npm run lint`, `npx --no-install tsc --noEmit`, `npm run build`, `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`, and `npm run local:restart`. The local app is refreshed at `http://127.0.0.1:3001`. No live TikHub/GPT/RunningHub/Feishu task was triggered by verification.

Follow-up on 2026-06-04: the user's "content is still empty" report was traced to `src/lib/content-pool.ts`, not TikHub transport. TikHub returned one normalized item for a live Xiaohongshu `灏忛箯G6` crawl, but `refreshProjectStats(...)` still removed Xiaohongshu records with temporary `sourceId` and no title/body. That residual ingest filter was deleted. `src/lib/tikhub.ts` now preserves wrapper fields when unwrapping nested records and includes `displayTitle` in Xiaohongshu title/text extraction. A live target-1 local crawl then completed with `returnedItems=1` and `projectTotalItems=1`.

Latest update on 2026-06-04: high-throughput application-level concurrency was implemented. New module `src/lib/concurrency.ts` defines shared pools with defaults crawl `8`, media `20`, GPT `50`, image `100`, Feishu `50`, and production `20`. GPT/image/Feishu have hard caps of `50`/`100`/`50`. TikHub HTTP calls, media cache fan-out, GPT text/tagging calls, image generation, Feishu CLI calls, simple-mode platform crawl, simple-mode production, and advanced batch production now use these bounded paths.

Simple-mode now crawls platforms and produces posts concurrently, while `createRunUpdateQueue(...)` serializes progress writes to `simple_runs`. Advanced batch production now uses the production pool and `createBatchJobUpdateQueue(...)` for serialized task status writes. `generateImagesFromPrompt(...)` selected-task concurrency is capped by `concurrencyConfig.image`, so simple mode can fan out up to its 9 selected images per post while the whole app stays under the global image pool.

Observed issue fixed during this update: the latest real simple run before the change had failed publishing with PostgreSQL `generated_posts_pkey` duplicate-key errors. Root cause was concurrent full-table replacement by `saveGeneratedPost(...)`; `src/lib/database.ts` now provides `saveGeneratedPostToDb(...)`, and `src/lib/generated-posts.ts` uses single-row upsert for generated-post saves.

Verification for this update passed on 2026-06-04: `node scripts/harness/concurrency_check.mjs`, `npm run lint`, `npx --no-install tsc --noEmit`, `npm run build`, `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`, and `npm run local:restart`. The local app is refreshed at `http://127.0.0.1:3001`. No live TikHub/OpenAI-compatible/RunningHub/Feishu production task was triggered by verification.

Next recommended work: run a small live manual simple-mode validation by submitting two keyword jobs close together and watching progress/Feishu publish behavior. For 500+ daily tasks, the next backend step is a durable PostgreSQL queue/worker model with row-level locking, likely `FOR UPDATE SKIP LOCKED`, so in-flight provider work can survive server restarts.

Latest update on 2026-06-04: per user instruction to delete the filtering mechanism after `灏忛箯P7` crawl failures, local keyword hard filtering and local Xiaohongshu note-type post-filtering were removed from `src/lib/tikhub.ts`. The current crawl path now relies on TikHub request parameters for search/sort/type, then locally dedupes, slices to `targetCount`, and caches media. Weibo no longer filters normalized results with local keyword matching. Xiaohongshu now maps UI note type to App V2 `note_type`, and no longer adds an all-type fallback unless the user selected all types.

Verification for this update passed on 2026-06-04: `node scripts/harness/keyword_relevance_check.mjs`, `node scripts/harness/xiaohongshu_note_type_check.mjs`, `npm run lint`, `npx --no-install tsc --noEmit`, `npm run build`, `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`, and elevated `npm run local:restart` after the known sandbox `spawn EPERM` limitation. The local app is refreshed at `http://127.0.0.1:3001`.

Historical note: earlier strict-filter debugging for Xiaohongshu keyword/type relevance is superseded by the current no-post-crawl-filter rule above.

Latest update on 2026-06-04: simple/advanced platform crawl preference synchronization was fixed in `src/app/page.tsx`. Advanced platform switches now persist the current platform controls into the shared workspace settings draft, and simple-mode start merges the currently visible advanced platform controls into the `settings` payload before calling `POST /api/simple/runs`. The backend path in `src/lib/simple-runs.ts` already consumes `settings.platformCrawlSettings`, so simple mode now receives Xiaohongshu, Douyin, Weibo, and WeChat Channels crawl sort/type settings configured in advanced mode.

Verification for this update passed without external production calls: a mocked Playwright browser check on `http://127.0.0.1:3001` intercepted `POST /api/simple/runs` and confirmed the payload carried Xiaohongshu `sort=time_descending` and `noteType=1`, Douyin `sort=2` and `contentType=3`, Weibo `sort=media`, `searchType=media`, `includeType=video`, `timeScope=week`, and WeChat Channels `sort=relevance`; `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1` passed; `npm run local:restart` refreshed the local app and local HTTP smoke passed. Do not use real `POST /api/simple/runs` as default verification because it triggers TikHub, text/image models, RunningHub, and Feishu.

Image-generation timeout and reference normalization policy was implemented on 2026-06-04 in `src/lib/image-generation.ts`. Selected image tasks now use a 180-second per-image deadline. If a selected image task times out, the backend records `Image task timed out; using source image` and returns the original source image URL for that slot, so slow image generation no longer fails the entire selected image batch. RunningHub upload, submit, and polling share this deadline. Reference images are normalized with `ffmpeg` before model input: local app-served images and downloadable remote images are converted to temporary JPG references with aspect ratio preserved and longest side capped at 2400px, then deleted after use. If a remote reference cannot be downloaded for normalization, the app logs that fact and falls back to the original remote URL.

Verification for this update passed: `ffmpeg -version`, `npm run lint`, `npx --no-install tsc --noEmit`, `npm run build`, elevated `npm run local:restart` after a sandbox `spawn EPERM`, and `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`. The local app at `http://127.0.0.1:3001` has been restarted and is serving the new image-generation strategy.

Latest read-only diagnosis on 2026-06-04: simple run `simple-1780536879346` for keyword `灏忛箯P7`, platform `xiaohongshu`, target `2`, failed in the `鐢熸垚鍥炬枃`/production stage because the local port-3001 server restarted at `2026-06-04T01:40:00.789Z` while RunningHub image tasks were still polling as `RUNNING`. Crawl succeeded with 2 candidates, AI tagging succeeded with 2 content tags and 14 visual tags, and production had already created 1 draft with 9 images. Feishu publish was skipped only because production was interrupted. `/api/config` confirmed the active runtime uses PostgreSQL plus RunningHub `nano-banana-pro`. The next engineering fix is a durable PostgreSQL-backed queue that persists provider task IDs and resumes polling after restart.

Simple-mode workspace ratio was adjusted on 2026-06-04 per user feedback: the `鑷姩浠诲姟鎵ц鐘舵€乣 panel should be the dominant desktop panel, and `涓€閿唴瀹圭敓浜 should be the narrower control panel. The current desktop grid in `src/app/globals.css` is `minmax(340px, 0.72fr) minmax(560px, 1.28fr)`. Playwright verification on `http://127.0.0.1:3001` observed approximate widths of 502px for the control panel and 892px for the status panel at 1440px viewport, with no horizontal overflow. Mobile remains a single-column stack with no horizontal overflow.

Simple-mode automatic task status layout was refined on 2026-06-04. In `src/app/page.tsx`, the simple run stage list now renders as four full-width vertical rows instead of a 2x2 grid. In `src/app/globals.css`, `.simple-stage-list` and the updated `.simple-stage-card` styles give each stage row a clear title/status, progress/message area, and percent column, with a mobile-safe two-column wrap. Verification passed: `npm run lint`, `npx --no-install tsc --noEmit`, `npm run build`, `npm run local:restart`, a Playwright layout check on `http://127.0.0.1:3001` confirming four stage cards in four unique vertical positions with no horizontal overflow or console errors, and `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`.

Live PostgreSQL migration was completed on 2026-06-04. Local PostgreSQL 18 service `postgresql-x64-18` is running on `127.0.0.1:5432`; `psql` is available at `D:\Program Files\PostgreSQL\18\bin\psql.exe`, not on PATH. A dedicated FluxPost Studio database/user were provisioned, and `.env.local` now contains `DATABASE_URL`; do not print or copy the connection string or password.

The SQLite-to-PostgreSQL copy was executed with `npm run db:migrate:postgres`. Migrated row counts were app_meta=2, content_projects=7, generated_posts=9, batch_jobs=3, material_folders=0, material_assets=0, execution_logs=300, crawl_jobs=19, runtime_posts=11, and simple_runs=4. PostgreSQL read-only verification after migration observed app_meta=3 because the migration script also writes `sqlite_to_postgres_migrated_at`.

`http://127.0.0.1:3001` was restarted with `npm run local:restart` after adding `DATABASE_URL`. Read-only `/api/config` now reports `databaseBackend=postgres` and `postgresConfigured=true`; local HTTP smoke and content-pool smoke passed. Full Harness baseline also passed with `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`.

RunningHub image provider integration was completed on 2026-06-03. The app now supports `OPENAI_IMAGE_ENDPOINT=runninghub` for `nano-banana-pro` image generation. `src/lib/image-generation.ts` uploads local app-served reference images to RunningHub, submits G-2 image tasks, polls `/openapi/v2/query`, and returns generated image URLs through the existing draft/image pipeline. `src/lib/config.ts`, `src/lib/types.ts`, `.env.example`, and README were updated. Local `.env.local` was updated with the RunningHub provider settings without exposing the API key.

The local server at `http://127.0.0.1:3001` was restarted after the RunningHub integration. Read-only `/api/config` confirmed `imageProvider=runninghub`, `imageModel=nano-banana-pro`, `runningHubConfigured=true`, and `runningHubBaseUrl=https://www.runninghub.cn`. This restart intentionally interrupted the older in-flight simple run that was still using the prior image provider.

Verification passed for this update: `npm run lint`, `npx --no-install tsc --noEmit`, `npm run build`, `npm run local:restart`, and `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`.

Simple-mode stuck-run diagnosis was completed on 2026-06-03. Latest run `simple-1780485506635` for keyword `灏忛箯P7` was confirmed `failed`, not still running. The root cause was a blocking simple-mode request that reached image generation, received repeated `400 I cannot fulfill this request` responses from `nano-banana-pro`, and was then interrupted when the old port-3001 Node process restarted during the request.

The mitigation is implemented in `src/lib/simple-runs.ts`, `src/app/api/simple/runs/route.ts`, and `src/app/page.tsx`: simple-run POST now creates the run and returns quickly while the local Node process continues the workflow in the background; the UI polls `/api/simple/runs` every three seconds while a run is queued/running; and `GET /api/simple/runs` marks queued/running runs as failed if they were last updated before the current server process started. This prevents interrupted old runs from staying stuck forever.

Verification passed for the stuck-run mitigation: `npm run lint`, `npx --no-install tsc --noEmit`, `npm run build`, `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`, and `npm run local:restart`. Read-only checks after restart confirmed `npm run local:watch-simple -- -Once` reports the latest run as `failed`, `/api/config` reports `imageModel=nano-banana-pro`, `databaseBackend=sqlite`, and Feishu configured, and `/api/simple/runs` returns the latest run statuses.

PostgreSQL runtime migration groundwork was completed on 2026-06-03. `src/lib/database.ts` now defaults to SQLite and switches to PostgreSQL when `DATABASE_URL` is configured. The `pg` dependency is installed; storage-facing callers were made async; `/api/config` now reports non-sensitive `databaseBackend` and `postgresConfigured`. PostgreSQL schema lives at `db/migrations/001_initial_postgres.sql`, and `npm run db:migrate:postgres` runs `scripts/db/migrate-sqlite-to-postgres.mjs` to copy SQLite rows into PostgreSQL without moving media binaries.

Verification passed for the PostgreSQL groundwork: `npm run db:migrate:postgres -- --dry-run`, `node scripts/harness/postgres_schema_check.mjs`, `npm run lint`, `npx --no-install tsc --noEmit`, `npm run build`, and `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`. The dry-run observed current SQLite row counts: app_meta=2, content_projects=7, generated_posts=8, batch_jobs=3, material_folders=0, material_assets=0, execution_logs=300, crawl_jobs=19, runtime_posts=10, simple_runs=3.

`http://127.0.0.1:3001` was refreshed on 2026-06-03 with `npm run local:restart` after the simple-mode stuck-run mitigation. The local production server is now serving the PostgreSQL-ready storage code and the background simple-run code.

Live PostgreSQL migration is complete for this local workspace. Current `http://127.0.0.1:3001` runtime reports `databaseBackend: "postgres"`.

Simple/advanced crawl preference sync and simple-mode layout resizing were completed on 2026-06-03. Workspace settings now include non-sensitive `platformCrawlSettings`; advanced crawl controls update them, advanced crawl persists the current platform preference before TikHub requests, and simple runs use the saved preferences. Douyin advanced controls include `Latest` sort and `Article` content type values. Douyin cookie is intentionally not stored as a default. Simple mode now gives the one-click generation panel the dominant desktop width and keeps the automatic task status/history as the narrower auxiliary column.

The image-generation model was switched on 2026-06-03 to `nano-banana-pro`. The user requested keeping the configured request address and API key unchanged. Updated `src/lib/config.ts`, `.env.example`, README examples, and local `.env.local` `OPENAI_IMAGE_MODEL`; do not expose `.env.local` contents.

Visual-tag image strategy prompt routing was updated on 2026-06-03 and expanded on 2026-06-11. Current routing: `APP` and `鍐呴グ绌洪棿` map to original-image reference without calling an image model; `姹借溅澶栬` and `杞﹀瀷缇庡浘` use the `carExterior` prompt/route; `甯︽枃瀛楀浘` uses `textImage`; and `浜鸿溅缇庡浘` uses `peopleWithCar`. The prompts are stored in workspace settings under `imageStrategyPrompts` with keys `carExterior`, `textImage`, and `peopleWithCar`; `imageWashPrompt` remains as the legacy/text-image compatible field. Simple mode and advanced production expose these prompt keys and reset actions. `src/lib/creation-controls.ts` owns the visual-tag routing and includes the shared instruction to preserve aspect ratio and scale the longest side to 2400px before image processing.

Simple-mode prompt editing was updated on 2026-06-03. The simple one-click workflow now shows editable text/content and image-washing prompts in `src/app/page.tsx`, with `浣跨敤榛樿` reset actions and a `淇濆瓨褰撳墠鎻愮ず璇嶄负榛樿` action. Starting a simple run validates both prompts, submits the current values, and the backend persists them through the existing workspace prompt settings path. Styling for the prompt editor lives in `src/app/globals.css`.

Simple-version automatic workflow steps 1-4 were implemented on 2026-06-03. `src/app/page.tsx` now defaults to a Simple workspace with a Simple/Advanced mode switch. Simple mode only asks for keyword, target count, and selected platforms, then calls `POST /api/simple/runs` to run crawl, AI tagging, content production, image processing, approval, and Feishu publish in one backend sequence. Advanced mode keeps the existing content, production, and material modules and now includes a `榛樿鐢熶骇绛栫暐` panel whose saved prompt/image settings are reused by simple mode.

The new simple run backend uses `src/lib/simple-runs.ts`, persists run history in SQLite table `simple_runs`, and stores shared default prompt settings through `src/lib/workspace-settings.ts` in `app_meta`. Feishu publishing now maps generated post `contentTags` to the `鍐呭鏍囩` field as a string array, suitable for a Base multi-select field.

The first real simple-mode task for keyword `灏忛箯GX`, target `2`, platform `xiaohongshu`, was observed running for several minutes. Local status showed crawl and AI tagging completed successfully, then production spent time in sequential image generation because one source exposed 12 video keyframes and each `gpt-image-2` call took roughly 70-238 seconds with one upstream `524` retry. `src/lib/simple-runs.ts` now caps simple-mode selected image/keyframe tasks to nine per generated post, and simple mode passes `taskConcurrency: 3` into `src/lib/image-generation.ts` so image washing runs with bounded concurrency. `scripts/local/watch-simple-runs.ps1` and `npm run local:watch-simple` were added for terminal-only backend visibility without adding a Web UI panel.

Video frame-first preview and tagging behavior was updated on 2026-06-03. For video or mixed source items with extracted `videoFrames`, `src/lib/source-tagging.ts` now tags frame assets only, `src/app/page.tsx` uses frames as the image preview list and hides the separate duplicate high-frame grid, and `src/lib/creation-controls.ts` creates default production image tasks from frames only. Preview images/covers are used only when the video has no extracted frames.

Douyin local media preview failure was fixed on 2026-06-03. The content pool already contained Douyin items with downloaded images/videos on disk, but `/media/crawl/douyin/...` returned 404 from `next start` because runtime-created files under `public/` were not reliably served by the production static layer. `next.config.ts` now rewrites `/media/crawl/:path*` and `/generated/:path*` before filesystem handling to `GET|HEAD /api/media/local/[...path]`, which streams files from the allowed local media roots and supports byte ranges for mp4 playback.

Weibo crawl response and image validation were fixed on 2026-06-03. `POST /api/crawl/jobs` now returns only the current crawl samples instead of top hot items from the whole keyword pool, and it filters cross-platform samples before tagging/ingest. The content-pool UI now stays filtered to the crawled platform after a crawl. Real smoke for keyword `灏忛箯GX` confirmed `includeType=pic` returns Weibo image posts with local downloaded images.

The local restart script was also fixed. Earlier `http://127.0.0.1:3001` could keep serving an old bundle because the script printed the npm parent PID while the real Node listener stayed alive. `scripts/local/restart.ps1` now checks native command exit codes, uses `netstat.exe` fallback, verifies port release, and prints the real listener PID.

Local media persistence/backfill has been added for crawled source images, videos, and keyframes. The next session should run one real keyword crawl from the Web UI, then select several content-pool items and run `琛ュ叏鏈湴绱犳潗` to confirm local image/video/keyframe counts improve for reachable remote URLs.

AI auto-tagging remains ready for real crawl review: confirm the OpenAI-compatible relay accepts text and image tagging inputs, and manually edit/save one content tag plus one visual tag.

Feishu publishing is adapted for the target Base fields `鍔ㄦ€佹爣棰榒, `鍔ㄦ€佹鏂嘸, and `鍔ㄦ€佺礌鏉恅; the UI now shows publish progress/status and the backend can optionally send a Feishu IM notification after successful Base publish.

On 2026-06-03 the first real simple-mode task for keyword `灏忛箯GX`, target `2`, platform `xiaohongshu`, finished as `partial`: crawl, tagging, and two generated posts succeeded, while Feishu publish failed because one post had image URLs but no local files suitable for Feishu attachment upload. A manual Feishu CLI task brief was sent with bot identity to the provided chat recipient. The exact recipient IDs are stored only in `.env.local` and must not be copied into Harness docs.

The Feishu attachment failure was fixed on 2026-06-03. `src/lib/feishu-cli.ts` now prepares attachment files before Base record creation: local app media paths still resolve under `public/`, and reachable remote HTTP(S) image URLs are downloaded into `public/generated/feishu-attachments` before upload. A real publish retry for the failed generated post created the Base record and uploaded 12 images to the `鍔ㄦ€佺礌鏉恅 attachment field. Automatic Feishu notification idempotency keys were shortened after one success notification failed field validation.

## Recovery Entry Point

- Work from project root: `C:\Users\Administrator\.codex\social-content-studio`.
- Latest simple-version automatic workflow files:
  - `src/lib/database.ts`
  - `db/migrations/001_initial_postgres.sql`
  - `scripts/db/migrate-sqlite-to-postgres.mjs`
  - `scripts/harness/postgres_schema_check.mjs`
  - `src/app/page.tsx`
  - `src/app/globals.css`
  - `src/lib/simple-runs.ts`
  - `src/lib/workspace-settings.ts`
  - `src/app/api/simple/runs/route.ts`
  - `src/app/api/workspace/settings/route.ts`
  - `src/lib/types.ts`
  - `src/lib/database.ts`
  - `src/lib/feishu-cli.ts`
  - `src/app/api/publish/feishu/route.ts`
  - `scripts/local/watch-simple-runs.ps1`
  - `package.json`
- Latest batch management files:
  - `src/app/page.tsx`
  - `src/app/globals.css`
  - `src/lib/content-pool.ts`
  - `src/lib/generated-posts.ts`
  - `src/app/api/content/items/batch/route.ts`
  - `src/app/api/production/posts/batch/route.ts`
- Latest AI source-tagging files:
  - `src/lib/types.ts`
  - `src/lib/source-tagging.ts`
  - `src/lib/content-pool.ts`
  - `src/app/api/crawl/jobs/route.ts`
  - `src/app/page.tsx`
  - `src/lib/creation-controls.ts`
- Latest media preview fix files:
  - `src/lib/media-request.ts`
  - `src/lib/media-cache.ts`
  - `src/lib/media-cache-status.ts`
  - `src/lib/media-backfill.ts`
  - `src/app/api/media/proxy/route.ts`
  - `src/app/api/media/local/[...path]/route.ts`
  - `next.config.ts`
  - `src/app/page.tsx`
- Latest Weibo crawl response fix files:
  - `src/app/api/crawl/jobs/route.ts`
  - `src/app/page.tsx`
  - `scripts/local/restart.ps1`
- Content-pool media backfill is exposed through `POST /api/content/items/batch` with `action: "cache_media"` and selected source item ids.
- The content-pool UI includes batch `琛ュ叏鏈湴绱犳潗`, selected-item `琛ュ叏褰撳墠绱犳潗`, mini source-card cache badges, and the selected-item `鏈湴绱犳潗缂撳瓨` status card.
- `mediaCache` on source items summarizes local images, remote fallback images, local video availability, keyframe count, and recent download errors.
- Remote browser image previews now use `/api/media/proxy?url=...` for HTTP(S) image URLs. Local `/media/...` and `/generated/...` paths still render directly.
- Local `/media/crawl/...` and `/generated/...` browser paths are served by beforeFiles rewrites to `/api/media/local/[...path]`; do not remove these rewrites unless another runtime media server replaces them.
- The local media API serves only `crawl` and `generated` roots, validates path segments, returns image/video content types, and supports HTTP Range requests for videos.
- Video/mixed items with `videoFrames` should treat frames as the visual source of truth: visual tagging, manual visual tag editing, image preview, and default production image tasks should use frames only.
- Video items with local `videoFrames` should use the extracted frames as the image preview instead of old remote signed cover fallbacks.
- `src/lib/source-tagging.ts` owns allowed tag normalization, GPT text/vision calls, first-nine visual asset collection, and failed/skipped tagging status.
- Crawled items are tagged after TikHub/media caching and before `ingestCrawlItems`.
- Manual content and visual tag edits are saved through `PATCH /api/content/items`; `src/lib/content-pool.ts` preserves user-edited tagging on recrawl.
- If the OpenAI-compatible relay rejects image inputs, visual tagging records a failed status without blocking content tags or ingest.
- Content-pool batch actions are status updates and destructive delete. Generated-post batch actions are status updates and destructive delete.
- Generated-post bulk approval/publish synchronizes source-item status through `markSourceRewritten`.
- Bulk actions write execution logs under `content/items` and `production/posts`.
- Batch selection controls now render as visible `閫夋嫨` / `宸查€塦 toggles in content-pool cards, production source cards, and generated-post cards.
- The local production server on `http://127.0.0.1:3001` was restarted after the visible-selection UI update.
- Use `npm run local:restart` after frontend or API changes that must be visible on `http://127.0.0.1:3001/`; `npm run build` alone does not refresh an already-running `next start` process.
- Simple-mode tasks are now submitted as background local Node work. This improves browser responsiveness but is not yet a durable queue: a process restart still interrupts active work, and the reconciler marks that interrupted run failed instead of resuming it.
- The simple-mode nine-image cap and bounded image concurrency change has passed build and Harness verification. After the first long-running simple run finished, `npm run local:restart` passed and `http://127.0.0.1:3001` now runs the refreshed code and env.
- `npm run local:watch-simple` can be run in a separate terminal to watch the latest simple run and backend execution logs. Use `npm run local:watch-simple -- -Once` for a single snapshot.
- `scripts/local/restart.ps1` must stop the real listener PID on port `3001`; it now falls back to `netstat.exe` when `Get-NetTCPConnection` misses the listener.
- Local startup helper files: `scripts/local/restart.ps1` and package scripts `dev:lan`, `start:lan`, `local:restart`.
- Latest implementation file: `src/lib/feishu-cli.ts`.
- Local `lark-cli` app configuration was verified with `lark-cli doctor`; bot identity is ready. Do not record the app id or secret in Harness docs.
- The target Feishu Base link was provided and parsed as:
  - Base token: configured locally and redacted from Harness docs
  - Table ID: `tblA0EfoAF9J4ffi`
- Bot read access to that Base is now confirmed with `lark-cli base +table-list --as bot`.
- Target table `tblA0EfoAF9J4ffi` is confirmed as `銆愭噦杞﹀笣銆戞帹閫佽〃`.
- Target fields are confirmed with `lark-cli base +field-list --as bot`:
  - `鍔ㄦ€佹爣棰榒: text, field id `fldhiSSxuy`.
  - `鍔ㄦ€佹鏂嘸: text, field id `fldTDGTnlc`.
  - `鍔ㄦ€佺礌鏉恅: attachment, field id `fld6L0XGw4`.
- `.env.local` has been updated with the required Feishu app publish target keys. Do not read or expose its contents.
- Default target fields are now:
  - `鍔ㄦ€佹爣棰榒
  - `鍔ㄦ€佹鏂嘸
  - `鍔ㄦ€佺礌鏉恅
- `鍔ㄦ€佺礌鏉恅 is treated as a Base attachment field. Publishing first creates the record with title/body, then uploads each generated local image file into that record's `鍔ㄦ€佺礌鏉恅 attachment cell.
- One generated post can upload multiple images to one `鍔ㄦ€佺礌鏉恅 cell.
- `src/lib/feishu-cli.ts` clears the specific broken proxy value `127.0.0.1:9` for Feishu CLI subprocesses.
- `src/lib/feishu-cli.ts` resolves `FEISHU_CLI_BIN=lark-cli` on Windows by invoking the npm-installed `@larksuite/cli/scripts/run.js` with `process.execPath`; this fixes web publish failures logged as `spawn lark-cli ENOENT`.
- `src/lib/feishu-cli.ts` maps root-relative app image URLs such as `/media/...` and `/generated/...` to project `public/` before checking `path.isAbsolute`; this fixes Feishu attachment preflight failures logged as `has image URLs but no local files`.
- Attachment preparation now runs before default Base record creation, so missing local attachments or unreachable remote image URLs should fail before creating a partial Feishu record.
- Remote HTTP(S) image URLs from image-model providers can be downloaded into `public/generated/feishu-attachments` during Feishu publish and then uploaded as local attachment files.
- `lark-cli base +record-batch-create --json @file` requires a relative path under the current working directory and strict UTF-8 without BOM; the wrapper passes relative JSON payload paths.
- Earlier UI progress implementation file: `src/app/page.tsx`.
- Latest review UI implementation files: `src/app/page.tsx` and `src/app/globals.css`.
- The production review panel includes `ReviewPackageCard`, which shows the selected generated post as a final review package with images or image placeholder, title, body, readiness badges, large preview, approval, and Feishu publish actions.
- `ReviewPackageCard` now shows a publish status panel when the user clicks `鍐欏叆椋炰功`, including running, success, warning, error, and notification status text.
- `src/lib/feishu-cli.ts` can send a success notification with `lark-cli im +messages-send --as bot` when `FEISHU_NOTIFY_CHAT_ID` or `FEISHU_NOTIFY_USER_ID` is configured. If both are configured, it sends to `FEISHU_NOTIFY_CHAT_ID`.
- Notification failure does not fail a successful Base write; it is returned to the frontend and recorded in the execution log details.
- `loadGeneratedPosts(preferredPostId?)` can keep the review panel focused on the newly generated, regenerated, edited, image-updated, or published post.

## Product And Runtime Facts

- UI has three top-level modules: content pool management, content production, and material management.
- Content pool management owns crawl tasks, keyword content pool, source preview, and source item CRUD.
- Content production owns queue filtering/sorting, batch selection, generation controls, generated post library, review, regeneration, and Feishu publish actions.
- Runtime data now lives primarily in `data/fluxpost.db`; legacy JSON files are retained as migration sources and compatibility artifacts.
- SQLite uses Node.js built-in `node:sqlite`; current local runtime requires Node.js 24+.
- Crawled media and generated images live under `public/media/` and `public/generated/`.
- Crawled media binaries should be persisted under `public/media/crawl`; the runtime database stores metadata, remote URLs, local app paths, and cache status.

## Important Boundaries

- Do not read or expose `.env.local` or any `.env*` secret values.
- Do not read or expose the local lark-cli config secret. `lark-cli config show` redacts the secret and is safe; raw config files may contain sensitive references.
- Do not run real Base writes unless the user is intentionally testing a publish or has selected approved content in the app.
- Do not trigger `POST /api/simple/runs` as a default verification step; it calls TikHub, OpenAI-compatible tagging/generation, image generation, and Feishu publishing.
- Do not run `npm run local:restart` while a real simple-mode request is still intentionally running unless the user accepts interrupting that request.
- Feishu attachment upload still passes local files to `lark-cli`, but the wrapper can now materialize reachable remote HTTP(S) image URLs into `public/generated/feishu-attachments` before upload. Expired or protected remote URLs still fail before Base record creation.
- Media backfill cannot recover already-expired or platform-protected remote URLs. Videos need a direct downloadable URL, and frames still require `ffmpeg`.
- Do not modify runtime data in `data/`, generated images in `public/generated/`, cached crawl media in `public/media/`, or debug screenshots/logs unless the user asks.
- Do not create a second persistent context system outside `docs/harness/`.
- For Xiaohongshu/content images, use `mergeDownloadedAndRemoteImages(..., { preferDownloaded: true })`; do not merge by slicing remote images with `downloadedImages.length`.
- Existing video-only Weibo records may have `images=[]` but populated `videoFrames`; use frames as visual preview when available.
- Weibo `includeType=all` can return text-only items with `images=[]`; use `includeType=pic` when specifically validating Weibo image extraction.
- Current local generated-post data may include drafts without images; in that case the final review package intentionally shows a `鏆傛棤鏈€缁堥厤鍥綻 placeholder.
- Feishu notification recipient values are optional env config. Do not read or expose `.env.local`; if no recipient is configured, the UI should report that notification was skipped. If both chat and user recipients are configured, the wrapper prefers the chat recipient.
- Latest real web publish failure before the resolver fix was `spawn lark-cli ENOENT`, not a Base permission error. Retest from the Web UI after the local app restart.
- Latest real web publish failure after the CLI resolver fix was root-relative image path resolution for `/media/...`; retest from the Web UI after the attachment path fix and local app restart.

## Recent Verification

- Passed on 2026-06-04 for fixed Xiaohongshu Web V3 search usage and diagnostics: `node scripts/harness/xiaohongshu_note_type_check.mjs`, `node scripts/harness/keyword_relevance_check.mjs`, `npx --no-install tsc --noEmit`, `npm run lint`, `npm run build`, `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`, and elevated `npm run local:restart` after sandbox `spawn EPERM`. Local HTTP smoke passed on `http://127.0.0.1:3001`. No real live TikHub/OpenAI-compatible/RunningHub/Feishu task was triggered.

- Passed on 2026-06-04 for read-only diagnosis of failed simple run `simple-1780551380562`: `GET /api/simple/runs`, `GET /api/activity?limit=300`, `GET /api/workspace/settings`, code inspection of `src/lib/simple-runs.ts` and `src/lib/tikhub.ts`, and `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`. No new live TikHub/OpenAI-compatible/RunningHub/Feishu task was triggered.

- Passed on 2026-06-04 for simple-mode Xiaohongshu image/text preference enforcement: `node scripts/harness/xiaohongshu_note_type_check.mjs`, `node scripts/harness/keyword_relevance_check.mjs`, `npm run lint`, `npx --no-install tsc --noEmit`, `npm run build`, `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`, `npm run local:watch-simple -- -Once`, and `npm run local:restart`. No real TikHub/OpenAI-compatible/RunningHub/Feishu task was triggered by this verification.

- Passed on 2026-06-04 for keyword relevance filtering and `灏忛箯P7` / `灏忛箯P7+` boundaries: `node scripts/harness/keyword_relevance_check.mjs`, `npm run lint`, `npx --no-install tsc --noEmit`, `npm run build`, `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`, `npm run local:watch-simple -- -Once`, and `npm run local:restart`. No real TikHub/OpenAI-compatible/RunningHub/Feishu task was triggered by the keyword verification or baseline.

- Passed on 2026-06-04 for live simple-run interruption and Xiaohongshu relevance diagnosis: `powershell -ExecutionPolicy Bypass -File scripts\local\restart.ps1 -SkipBuild`, `npm run local:watch-simple -- -Once`, read-only `GET /api/content-pool`, read-only `GET /api/workspace/settings`, and code inspection. No new crawl/model/Feishu task was started during the diagnosis.

- Passed on 2026-06-04 for simple/advanced crawl preference sync: mocked Playwright request-body check on `http://127.0.0.1:3001`, `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`, and `npm run local:restart`. No real TikHub/OpenAI-compatible/RunningHub/Feishu simple task was triggered by the mocked browser check.

- Passed on 2026-06-03 for simple-mode stuck-run mitigation: `npm run lint`, `npx --no-install tsc --noEmit`, `npm run build`, `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`, and `npm run local:restart`.
- Passed on 2026-06-03 read-only checks after restart: `npm run local:watch-simple -- -Once`, `GET http://127.0.0.1:3001/api/config`, and `GET http://127.0.0.1:3001/api/simple/runs`. No new TikHub/OpenAI/Feishu call was triggered.

- Passed on 2026-06-03 for simple/advanced crawl preference sync and simple layout resizing: `npm run lint`, `npx --no-install tsc --noEmit`, `npm run build`, `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`, elevated `npm run local:restart`, Playwright desktop UI width/no-overflow check, Playwright advanced Douyin `Latest` sort and `Article` content-type check, and read-only `GET /api/workspace/settings` confirming `platformCrawlSettings`.

- Passed on 2026-06-03 image model switch to `nano-banana-pro`: `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`, `npm run local:restart`, `node scripts/harness/http_smoke.js http://127.0.0.1:3001`, and read-only `GET /api/config` confirmed the running local app reports `imageModel=nano-banana-pro`.

- Passed on 2026-06-03 custom visual-tag image prompt recheck: `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`, `npm run local:watch-simple -- -Once`, `node scripts/harness/http_smoke.js http://127.0.0.1:3001`, and read-only `GET /api/workspace/settings` on `http://127.0.0.1:3001` confirmed the live local app exposes `imageStrategyPrompts.carExterior`, `imageStrategyPrompts.textImage`, and `imageStrategyPrompts.peopleWithCar`.
- Passed on 2026-06-03 for visual-tag image strategy prompt routing: `npm run lint`, `npx --no-install tsc --noEmit`, `npm run build`, `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`, and `npm run local:restart`.
- Passed on 2026-06-03 browser/API smoke on `http://127.0.0.1:3001`: simple mode shows one text prompt plus three image strategy prompts and reset buttons with no horizontal overflow; advanced production shows the three image strategy prompts and original-reference rule with no horizontal overflow; `/api/workspace/settings` returns `carExterior`, `textImage`, and `peopleWithCar` prompt keys.

- Passed on 2026-06-03 for simple-mode editable prompt settings: `npm run lint`, `npx --no-install tsc --noEmit`, `npm run build`, `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`, and `npm run local:restart`.
- Passed on 2026-06-03 browser UI smoke on `http://127.0.0.1:3001`: desktop `1440x950` and mobile `390x844` confirmed two simple-mode prompt textareas, two reset buttons, the prompt-save action, no console errors, and no horizontal overflow.

- Passed on 2026-06-03 for simple-version automatic workflow UI and APIs: `npm run lint`, `npx --no-install tsc --noEmit`, `npm run build`, `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`, and `npm run local:restart`.
- Passed on 2026-06-03 browser UI smoke on `http://127.0.0.1:3001`: simple workspace, keyword field, simple start button, advanced module switcher after mode change, and no horizontal overflow on desktop `1440x950` or mobile `390x844`.
- Passed on 2026-06-03 for simple-run slowness mitigation and terminal backend watcher: `npm run local:watch-simple -- -Once`, `npm run lint`, `npx --no-install tsc --noEmit`, `npm run build`, and `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`.
- Passed on 2026-06-03 for simple-mode nine-image cap and bounded image concurrency: `npm run lint`, `npx --no-install tsc --noEmit`, `npm run build`, `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`, and `npm run local:watch-simple -- -Once`. The existing `http://127.0.0.1:3001` app was not restarted because a real simple run was still running on the old sequential code path.
- Passed on 2026-06-03 for Feishu notification env and recipient precedence: `.env.local` updated without exposing values, `src/lib/feishu-cli.ts` prefers chat notification when both chat/user env values exist, `npm run lint`, `npx --no-install tsc --noEmit`, `npm run build`, `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`, manual Feishu CLI task brief send, and `npm run local:restart`.
- Passed on 2026-06-03 for Feishu remote image attachment materialization: `npm run lint`, `npx --no-install tsc --noEmit`, `npm run build`, `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`, real publish retry uploaded 12 remote image-model URLs as Feishu attachments, manual Feishu repair brief send, and `npm run local:restart`.
- Passed on 2026-06-03 for video frame-first preview/tagging/task behavior: `npm run lint`, `npx --no-install tsc --noEmit`, `npm run build`, `npm run local:restart`, and `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`.
- Passed on 2026-06-03 browser check on `http://127.0.0.1:3001`: Douyin sample `douyin-7639960163977784475` visible preview loaded `/frames/cover.jpg` and no visible original `image-*` source image in the current viewport.

- Passed on 2026-06-03 for runtime local media serving: `npm run lint`, `npx --no-install tsc --noEmit`, `npm run build`, `npm run local:restart`, and `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`.
- Passed on 2026-06-03 real Douyin crawl smoke: platform `douyin`, keyword `灏忛箯GX`, `targetCount=1`, returned one completed item with 12 local images, a local mp4, 12 local frames, and `mediaCache.status=local_complete`.
- Passed on 2026-06-03 HTTP media checks on `http://127.0.0.1:3001`: Douyin local image and frame returned 200, Douyin local mp4 returned 200, and a video byte-range request returned 206.
- Passed on 2026-06-03 browser media check on `http://127.0.0.1:3001`: Chromium loaded a Douyin source image, cover frame, and mp4 metadata from `/media/crawl/...`.

- Passed on 2026-06-03 for Weibo crawl response fix: `npm run lint`, `npx --no-install tsc --noEmit`, `npm run build`, and `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`.
- Passed on 2026-06-03 after refreshing `http://127.0.0.1:3001`: `powershell -ExecutionPolicy Bypass -File scripts\local\restart.ps1 -SkipBuild`; old listener PID `44656` was stopped and new listener PID `35992` started.
- Passed on 2026-06-03 real Weibo crawl smoke: platform `weibo`, keyword `灏忛箯GX`, `targetCount=1`, `includeType=all` returned only Weibo items.
- Passed on 2026-06-03 real Weibo image crawl smoke: platform `weibo`, keyword `灏忛箯GX`, `targetCount=3`, `includeType=pic` returned three Weibo image posts with local downloaded image counts `5`, `3`, and `3`.
- Passed on 2026-06-03 for local media persistence/backfill: `npm run lint`, `npx --no-install tsc --noEmit`, `npm run build`, `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`, and `npm run local:restart`.
- Passed on 2026-06-03 API smoke: `POST /api/content/items/batch` with `action=cache_media` and a missing id returned `updatedCount=0` and one not-found id.
- Passed on 2026-06-03 Playwright smoke on `http://127.0.0.1:3001/`: `琛ュ叏鏈湴绱犳潗` and `鏈湴绱犳潗缂撳瓨` render with no console errors and no horizontal overflow.
- Passed on 2026-06-03 for remote media proxy preview fix: `npm run lint`, `npx --no-install tsc --noEmit`, `npm run build`, `npm run local:restart`, and `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`.
- Passed on 2026-06-03 browser check on `http://127.0.0.1:3001`: selected Xiaohongshu item `xiaohongshu-6a0db7220000000006020fad`; visible proxied remote images loaded with dimensions and no broken visible images.
- Passed on 2026-06-03 for AI source tagging: `npm run lint`, `npx --no-install tsc --noEmit`, `npm run build`, and `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`.
- Passed on 2026-06-03 after restarting the local app: `npm run local:restart`; local HTTP smoke passed on `http://127.0.0.1:3001`.
- Passed on 2026-06-03 browser check on `http://127.0.0.1:3001`: Playwright switched to project `灏忛箯GX` and confirmed `AI 鏍囩`, `鍐呭鏍囩`, and `鍥剧墖 / 鍏抽敭甯ф爣绛綻 render with no console errors.
- Passed on 2026-06-02 for the local production refresh script: `npm run local:restart`.
- Passed on 2026-06-02 for batch resource management: `npm run lint`, `npx --no-install tsc --noEmit`, `npm run build`, and `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`.
- Passed on 2026-06-02 for the running local app on `http://127.0.0.1:3001`: `node scripts/harness/http_smoke.js http://127.0.0.1:3001`.
- Passed on 2026-06-02 for visible batch selection controls: Playwright confirmed content-pool selection changes from `閫夋嫨` to `宸查€塦, generated-post selection changes from `閫夋嫨` to `宸查€塦, and batch counters update.
- Passed on 2026-06-02 after restarting `http://127.0.0.1:3001`: `node scripts/harness/http_smoke.js http://127.0.0.1:3001`.
- Passed on 2026-06-02 after user granted Base access: `lark-cli base +table-list --as bot --base-token ... --limit 50`.
- Passed on 2026-06-02 after user granted Base access: `lark-cli base +field-list --as bot --base-token ... --table-id tblA0EfoAF9J4ffi --limit 200`.
- Passed on 2026-06-02 for Feishu record dry-run with `鍔ㄦ€佹爣棰榒 and `鍔ㄦ€佹鏂嘸; no real data was written.
- Passed on 2026-06-02 for Feishu attachment upload dry-run against `鍔ㄦ€佺礌鏉恅; no file was uploaded.
- Passed on 2026-06-02 after restarting the local app on `http://127.0.0.1:3001`: local HTTP smoke and `/api/config` Feishu configured check.
- Passed on 2026-06-02 for Feishu dynamic field dry-run using `鍔ㄦ€佹爣棰榒 and `鍔ㄦ€佹鏂嘸; no real data was written.
- Passed on 2026-06-02 for Feishu attachment publish wrapper update: `npm run lint`, `npx --no-install tsc --noEmit`, `npm run build`, and `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`.
- Passed on 2026-06-02 for Feishu CLI readiness: `lark-cli config show`, `lark-cli doctor --offline`, and `lark-cli doctor` with `LARK_CLI_NO_PROXY=1` and proxy env values cleared.
- Passed on 2026-06-02 for the final review package UI: `npm run lint`, `npx --no-install tsc --noEmit`, `npm run build`, Playwright desktop/mobile visibility and overflow checks, `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`, and `node scripts/harness/http_smoke.js http://127.0.0.1:3001`.
- Passed on 2026-06-02 for Feishu publish feedback and optional notification: `npm run lint`, `npx --no-install tsc --noEmit`, `npm run build`, `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`, Playwright mocked publish check, and `node scripts/harness/http_smoke.js http://127.0.0.1:3001`.
- Passed on 2026-06-02 for the Windows `lark-cli` executable resolution fix: `npm run lint`, `npx --no-install tsc --noEmit`, `npm run build`, `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`, and `node scripts/harness/http_smoke.js http://127.0.0.1:3001` after restart.
- Passed on 2026-06-02 for the Feishu attachment root-relative path fix: `npm run lint`, `npx --no-install tsc --noEmit`, local `/media/...` path-mapping check, `npm run build`, `powershell -ExecutionPolicy Bypass -File scripts/harness/check.ps1`, and `node scripts/harness/http_smoke.js http://127.0.0.1:3001` after restart.
- Next build emitted the known Turbopack tracing warnings for `src/lib/media-cache.ts` and `next.config.ts`; build still completed successfully.
