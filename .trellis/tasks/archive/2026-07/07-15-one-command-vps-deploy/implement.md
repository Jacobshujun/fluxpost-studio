# Implementation

- [x] Parameterize Compose app port/domain while preserving the legacy proxy-on upgrade path.
- [x] Update `vps-deploy.sh` to select private or HTTPS services and derive health checks safely.
- [x] Add the Ubuntu 24.04 bootstrap with validation, official Docker install, atomic mode-0600 env generation, wrapper installation, and one-command deployment.
- [x] Add the domain enablement command with hostname validation and persisted deployment keys.
- [x] Add deterministic deployment contract coverage to the Trellis baseline.
- [x] Update deployment example and beginner-facing deployment documentation.
- [x] Run focused checks, Bash syntax, YAML parsing, private/HTTPS/legacy plan execution, type-check, lint, build, and full Trellis baseline.
- [x] Commit scoped files and push GitHub `main` as `0cdae8a`.
