# 更新说明

## 2026-09-15

本次整理 2026-09-07 至 2026-09-15 的 41 个本地提交，包含功能、修复、验证和任务归档。代码范围为 `45462de` 之后至 `9dba663`；本更新说明及同步记录另行提交。

### Canvas 工作流与批量任务

- 新增按来源组织的内容采集批次：每个链接或 ID 采集一次，文本、图片、视频等输出可在下游节点复用，支持失败恢复和结果汇总。默认不执行 AI 打标。(`c482338`)
- 新增逐图迭代区域：复用内部文字和图片流程，保留图片顺序与来源，支持失败项重试、共享输出和显式刷新下游结果；修复连接点刷新和嵌套节点删除。(`c6539d6`, `66fc481`)
- 批量文字参数支持以独占一行的 `---` 分隔多段提示词，保留段内换行；修复手动列表编辑时换行丢失，已有列表仍默认每行一个值。(`90d0ad6`, `b13c319`)
- 内容合成节点新增默认开启的审核开关；需要审核时保存为草稿，跳过审核时标记为已通过，重跑保留已发布状态。(`da3696c`)
- 图片库节点在选择筛选条件后再加载，使用每页 24 张的分页与缩略图，减少初始加载和重复渲染；修复节点列表文字对齐。(`9d47c0a`, `ef89326`, `75a7ef4`, `94fd190`)
- 修复遮罩边缘漏出原图，以及视频遮罩结果无法预览的问题。(`dc1437c`, `8001886`)

### 图片生成与配置

- 简单模式改用与 Canvas 一致的图片比例和分辨率档位选择；对精确像素请求增加结果校验，不再静默替换尺寸或缩放成品。ToAPIs 当前使用比例与档位，不支持提交自定义精确像素。(`2fda0e0`)
- 对齐 ToAPIs 普通版 GPT Image 2.5 Flare/Sunburst 请求：固定高质量、每次一张，使用 `reference_images`，按需传递透明背景；恢复 4K 的 1:1、4:3、3:4 选项，切换档位保留比例。4K 是档位，正方形示例为 2880×2880。(`67dadae`)
- 简单模式新增画质和背景选择，并验证透明背景与输出格式的组合。具体字段是否发送取决于服务商：普通版 ToAPIs 2.5 不提供格式、压缩率控制，旧模型保留原有请求协议。(`3a48b1c`, `a616e69`, `67dadae`)
- 简单模式图片生成失败时丢弃对应图片位，保留成功结果；全部失败时明确报错，避免自动混入未经生成的源图。(`f98e50a`)
- 图片请求新增 Xray 代理开关，关闭时保留代理地址；补充 ToAPIs 中国域名识别。(`5db683c`, `620012e`)
- 修复保存配置后 API 路由和后台任务读取旧配置的问题，同一进程内后续请求使用更新后的配置对象。(`40140f9`)

### 素材库、审核台与文案库

- 图集选择器支持搜索，新增图集之间的批量移动，保留素材在其他图集中的归属。(`a3b194e`)
- 图片预览和详情支持删除；预览支持左右方向键切图、Delete 删除和 Escape 关闭，输入框及只读素材有对应保护。删除图片不再弹出确认，成功后继续预览下一张，无下一张时尝试上一张。(`c72631e`, `005c20b`, `482c903`, `9dba663`)
- 审核台改用分页列表、独立详情与缩略图，减少整表传输和保存开销；保留跨页勾选，防止延迟保存覆盖新输入，审核通过后继续停留在当前稿件。(`d856e3b`)
- 修复文案库新建草稿被列表加载结果覆盖的问题，保留新草稿编辑状态。(`061be8b`)
- 内容采集支持跳过 AI 打标，便于先采集、后整理。(`67c4c81`)

### 验证与适用范围

- 本次同步前的完整离线基线结果记录于 `.trellis/tasks/archive/2026-09/09-15-github-update-notes/verification.md`。
- 既有桌面、移动端模拟浏览器和隔离数据库验证记录保留在 `.trellis/tasks/archive/2026-09/`。这些结果不代表真实服务商成功率或生产环境性能。
- 本次仅同步 GitHub 代码和文档，不启动本地新候选版本、不部署生产，也不执行付费生成或飞书发布。功能的真实服务商及人工验收状态仍以 `.trellis/spec/fluxpost/feature_list.json` 为准。

[查看本次代码差异](https://github.com/Jacobshujun/fluxpost-studio/compare/45462de7c63363c3bce1c3fc07233a4021ef0d81...9dba663e29b99fbe9f14837a820cb90f95cc53e6)
